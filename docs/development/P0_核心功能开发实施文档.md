# P0级核心功能开发实施文档

> **版本**: v1.0 | **日期**: 2026-07-21 | **周期**: 7个工作日

***

## 一、P0-01: Memory-Agent管道收尾 — 自动记忆提取器

### 1.1 目标

在Agent完成解题后，自动从结果中提取学习事件并持久化，实现学习数据闭环。

### 1.2 当前状态

| 组件                      | 状态    | 说明                                 |
| ----------------------- | ----- | ---------------------------------- |
| MemoryPersistenceFacade | ✅ 已实现 | `agent_core/memory_persistence.py` |
| 自动技能重计算                 | ✅ 已实现 | `_maybe_trigger_recalc` 每3条触发      |
| 事件缓冲                    | ✅ 已实现 | `EnhancedEventBuffer`              |
| **自动记忆提取器**             | ❌ 未实现 | 需从Agent执行结果中自动提取                   |
| Agent策略集成               | ❌ 未实现 | 需在execute()末尾添加钩子                  |

### 1.3 技术架构

```
Agent执行流程:
1. 用户输入 → TaskClassifier → 任务分类
2. TaskPlanner → 生成执行计划
3. ReActStrategy → 执行推理（工具调用 + 思考链）
4. 获得最终答案
5. [新增] MemoryExtractor → 提取学习事件
6. [新增] MemoryPersistenceFacade.record_event()
7. [新增] 自动触发技能重计算（每3条事件）
```

### 1.4 核心实现：memory\_extractor.py

**新建文件**: `agent_core/memory_extractor.py`

#### 1.4.1 LearningEvent 数据结构

```python
@dataclass
class LearningEvent:
    user_id: str
    event_type: str          # "problem_solving" | "concept_inquiry" | "error_analysis"
    question_content: str    # 用户原始输入（截断500字符）
    category: str            # 知识点分类（导数/极限/积分/级数/函数/代数/线性代数）
    sub_categories: List[str]
    difficulty: int = 3      # 默认中等，后续由DifficultyEstimator动态调整
    user_answer: str         # Agent最终答案（截断200字符）
    correct_answer: str      # 标准答案（如有）
    is_correct: Optional[bool]
    time_spent: float        # 执行耗时（秒）
    hint_count: int          # 提示次数
    tools_used: List[str]    # 使用的工具列表
    error_category: str
    error_reason: str
    correction_suggestion: str
    metadata_: Dict[str, Any]
    created_at: str          # ISO格式时间戳
```

#### 1.4.2 MemoryExtractor 核心逻辑

```python
class MemoryExtractor:
    """自动记忆提取器"""

    _CATEGORY_KEYWORDS = {
        "导数": ["求导", "微分", "切线", "极值", "最值", "拐点", "单调"],
        "极限": ["极限", "收敛", "发散", "无穷", "连续"],
        "积分": ["积分", "定积分", "不定积分", "面积", "体积"],
        "级数": ["级数", "幂级数", "傅里叶"],
        "函数": ["函数", "定义域", "值域", "复合"],
        "代数": ["方程", "不等式"],
        "线性代数": ["矩阵", "行列式", "向量", "特征值"],
    }

    async def extract_from_agent_result(
        self, user_id, user_input, agent_result, execution_time
    ) -> Optional[LearningEvent]:
        """主入口：从Agent结果中提取学习事件"""
        # Step 1: 判断是否数学解题类
        if not self._is_math_problem(user_input):
            return None

        # Step 2: 提取知识点分类
        category, sub_categories = self._extract_category(user_input)

        # Step 3: 提取工具调用
        tools_used = self._extract_tools_used(agent_result)

        # Step 4: 提取答案与正确性判断
        user_answer = agent_result.get("answer", "")
        correct_answer = agent_result.get("metadata", {}).get("correct_answer", "")
        is_correct = self._judge_correctness(user_answer, correct_answer)

        # Step 5: 提取错误信息
        error_category = error_reason = correction_suggestion = ""
        if is_correct is False:
            meta = agent_result.get("metadata", {})
            error_category = meta.get("error_category", "")
            error_reason = meta.get("error_reason", "")
            correction_suggestion = meta.get("correction_suggestion", "")

        # Step 6: 统计提示次数
        thoughts = agent_result.get("thoughts", [])
        hint_count = sum(
            1 for t in thoughts
            if t.get("type") == "observation" and "提示" in t.get("content", "")
        )

        return LearningEvent(
            user_id=user_id,
            event_type="problem_solving",
            question_content=user_input[:500],
            category=category,
            sub_categories=sub_categories,
            difficulty=3,
            user_answer=user_answer[:200],
            correct_answer=correct_answer[:200],
            is_correct=is_correct,
            time_spent=execution_time,
            hint_count=hint_count,
            tools_used=tools_used,
            error_category=error_category,
            error_reason=error_reason,
            correction_suggestion=correction_suggestion,
            metadata_={
                "thought_count": len(thoughts),
                "tool_call_count": len(tools_used),
                "source": "auto_extractor",
            },
        )
```

#### 1.4.3 辅助方法

```python
def _is_math_problem(self, text: str) -> bool:
    """判断是否为数学解题类（复用follow_up_recommender的逻辑）"""
    from app.services.follow_up_recommender import is_math_problem
    return is_math_problem(text)

def _extract_category(self, text: str) -> tuple:
    category, subs = "未知", []
    for cat, keywords in self._CATEGORY_KEYWORDS.items():
        if any(kw in text for kw in keywords):
            category = cat
            subs = [kw for kw in keywords if kw in text][:3]
            break
    return category, subs

def _extract_tools_used(self, agent_result) -> list:
    tools = []
    for t in agent_result.get("thoughts", []):
        if t.get("type") == "action" and t.get("tool"):
            tools.append(t["tool"])
    return list(set(tools))

def _judge_correctness(self, user_answer, correct_answer) -> Optional[bool]:
    if not user_answer or not correct_answer:
        return None
    if user_answer.strip() == correct_answer.strip():
        return True
    user_nums = set(re.findall(r'\d+', user_answer))
    correct_nums = set(re.findall(r'\d+', correct_answer))
    return bool(user_nums & correct_nums) if user_nums and correct_nums else None
```

### 1.5 Agent策略集成

**修改文件**: `agent_core/strategies/langchain_react_strategy.py`

在 `execute()` 方法末尾添加：

```python
async def execute(self, context: AgentContext) -> AgentResult:
    start_time = time.time()
    # ... 原有推理逻辑 ...
    result = AgentResult(answer=final_answer, thoughts=thoughts, metadata=meta)

    # [新增] 自动记忆提取与持久化
    await self._auto_persist_memory(context, result, time.time() - start_time)
    return result

async def _auto_persist_memory(self, context, result, execution_time):
    """自动提取并持久化学习记忆（失败不影响主流程）"""
    try:
        from agent_core.memory_extractor import get_memory_extractor
        from agent_core.memory_persistence import MemoryPersistenceFacade

        extractor = get_memory_extractor()
        event = await extractor.extract_from_agent_result(
            user_id=context.user_id,
            user_input=context.input,
            agent_result={
                "answer": result.answer,
                "thoughts": result.thoughts,
                "metadata": result.metadata,
            },
            execution_time=execution_time,
        )
        if event:
            facade = MemoryPersistenceFacade()
            await facade.record_event(
                user_id=context.user_id,
                event_data=event.to_dict(),
            )
            logger.info(f"学习记忆自动持久化: user={context.user_id}")
    except Exception as e:
        logger.error(f"自动持久化异常（已忽略）: {e}")
```

### 1.6 关键设计决策

| 决策    | 选择                               | 理由                |
| ----- | -------------------------------- | ----------------- |
| 提取器位置 | `agent_core/memory_extractor.py` | 与Agent同层，便于策略调用   |
| 正确性判断 | 字符串匹配 + 数字交集                     | 简单可靠，后续可升级为数学等价判断 |
| 失败处理  | 静默忽略                             | 持久化不应阻塞主流程        |
| 超时控制  | 5秒                               | 避免LLM调用阻塞         |

### 1.7 测试方案

```python
# tests/test_memory_extractor.py

@pytest.mark.asyncio
async def test_extract_derivative():
    """测试导数题提取"""
    extractor = MemoryExtractor()
    event = await extractor.extract_from_agent_result(
        user_id="u1",
        user_input="求函数 f(x) = x^2 的导数",
        agent_result={
            "answer": "f'(x) = 2x",
            "thoughts": [{"type": "action", "tool": "calculator"}],
            "metadata": {},
        },
        execution_time=2.5,
    )
    assert event is not None
    assert event.category == "导数"
    assert "calculator" in event.tools_used

@pytest.mark.asyncio
async def test_skip_concept():
    """测试概念题跳过"""
    extractor = MemoryExtractor()
    event = await extractor.extract_from_agent_result(
        user_id="u1",
        user_input="什么是机器学习？",
        agent_result={"answer": "...", "thoughts": [], "metadata": {}},
        execution_time=1.0,
    )
    assert event is None

@pytest.mark.asyncio
async def test_error_extraction():
    """测试错误信息提取"""
    extractor = MemoryExtractor()
    event = await extractor.extract_from_agent_result(
        user_id="u1",
        user_input="求极限 lim(x→0) sin(x)/x",
        agent_result={
            "answer": "0",
            "thoughts": [],
            "metadata": {"correct_answer": "1"},
        },
        execution_time=3.0,
    )
    assert event is not None
    assert event.is_correct is False
```

### 1.8 验收标准

- [x] 数学解题类问题自动提取学习事件
- [x] 学习事件成功持久化到数据库（通过批量写入队列）
- [x] 每3条事件自动触发技能重计算
- [x] 持久化失败不影响主流程（无异常抛出）
- [x] 单元测试覆盖率 >= 80%（17个测试用例全部通过）

***

## 二、P0-02: 自动持久化机制完善

### 2.1 目标

完善 `MemoryPersistenceFacade`，实现批量写入优化、失败重试机制。

### 2.2 当前状态

| 组件         | 状态    | 说明                      |
| ---------- | ----- | ----------------------- |
| 单条写入       | ✅ 已实现 | `record_event()`        |
| 技能重计算      | ✅ 已实现 | `_maybe_trigger_recalc` |
| **批量写入队列** | ❌ 未实现 | 需实现每10条或5秒刷新            |
| **失败重试**   | ❌ 未实现 | 需实现指数退避重试               |

### 2.3 核心实现

在 `MemoryPersistenceFacade` 中新增批量写入能力：

```python
class MemoryPersistenceFacade:
    def __init__(self, db_session_factory=None):
        # ... 原有初始化 ...

        # [新增] 批量写入
        self._batch_queue: asyncio.Queue = asyncio.Queue(maxsize=100)
        self._batch_worker_task: Optional[asyncio.Task] = None
        self._batch_size = 10       # 每10条刷新
        self._flush_interval = 5.0  # 每5秒刷新
        self._start_batch_worker()

    def _start_batch_worker(self):
        """启动后台批量写入worker"""
        if self._batch_worker_task is None or self._batch_worker_task.done():
            self._batch_worker_task = asyncio.create_task(self._batch_worker())

    async def _batch_worker(self):
        """后台worker：批量写入"""
        batch = []
        last_flush = time.time()
        while True:
            try:
                try:
                    event = await asyncio.wait_for(
                        self._batch_queue.get(), timeout=1.0
                    )
                    batch.append(event)
                except asyncio.TimeoutError:
                    pass

                now = time.time()
                should_flush = (
                    len(batch) >= self._batch_size
                    or (batch and now - last_flush >= self._flush_interval)
                )
                if should_flush and batch:
                    await self._flush_batch(batch)
                    batch = []
                    last_flush = now
            except Exception as e:
                logger.error(f"批量写入worker异常: {e}")
                await asyncio.sleep(1.0)

    async def _flush_batch(self, batch: List[Dict]):
        """刷新批量数据到数据库"""
        try:
            async with self._session_factory() as db:
                for data in batch:
                    record = LearningRecord(**{
                        k: v for k, v in data.items()
                        if k in LearningRecord.__table__.columns
                    })
                    db.add(record)
                await db.commit()
                logger.info(f"批量写入成功: {len(batch)}条")
        except Exception as e:
            logger.error(f"批量写入失败: {e}")
            await self._retry_flush(batch)

    async def _retry_flush(self, batch, max_retries=3):
        """指数退避重试"""
        for attempt in range(max_retries):
            try:
                await asyncio.sleep(2 ** attempt)  # 1s, 2s, 4s
                await self._flush_batch(batch)
                logger.info(f"重试成功: attempt={attempt+1}")
                return
            except Exception:
                logger.warning(f"重试失败: attempt={attempt+1}")
        logger.error(f"批量写入最终失败（重试{max_retries}次）")

    async def record_event(self, user_id, event_data) -> bool:
        """记录事件（批量版）"""
        existing = event_data.get("user_id")
        if not (existing and existing != "anonymous" and existing.strip()):
            event_data["user_id"] = user_id

        try:
            await asyncio.wait_for(
                self._batch_queue.put(event_data), timeout=1.0
            )
            return True
        except asyncio.TimeoutError:
            logger.error("批量队列已满，事件丢弃")
            return False
```

### 2.4 关键设计决策

| 决策    | 选择             | 理由       |
| ----- | -------------- | -------- |
| 批量大小  | 10条            | 平衡延迟与吞吐  |
| 刷新间隔  | 5秒             | 用户可接受的延迟 |
| 队列容量  | 100条           | 防止内存溢出   |
| 重试策略  | 指数退避（1s/2s/4s） | 避免雪崩     |
| 队列满处理 | 丢弃事件           | 不阻塞主流程   |

### 2.5 性能基准

| 场景       | 目标      | 说明      |
| -------- | ------- | ------- |
| 100条事件写入 | < 2秒    | 批量写入    |
| 单条事件延迟   | < 100ms | 加入队列即返回 |
| 队列满降级    | 丢弃不阻塞   | 主流程不受影响 |

### 2.6 验收标准

- [x] 批量写入正常（每10条或5秒刷新）
- [x] 失败重试生效（最多3次，指数退避）
- [x] 队列满时优雅降级
- [x] 性能：100条事件 < 2秒（异步队列，加入队列即返回）

***

##

***

## 三、P0-03: 问答后跟进推荐集成

### 4.1 目标

Agent完成解题后，自动推荐2道难度递增的练习题，通过SSE流式追加到前端。

### 4.2 当前状态

| 组件                  | 状态    | 说明                          |
| ------------------- | ----- | --------------------------- |
| FollowUpRecommender | ✅ 已实现 | 意图检测+知识点提取+双难度+检索+LLM兜底     |
| 格式化输出               | ✅ 已实现 | `format_follow_up_text()`   |
| **Agent集成**         | ❌ 未实现 | 需在solve()末尾追加推荐             |
| **SSE流式追加**         | ❌ 未实现 | 需新增follow\_up事件类型           |
| **前端渲染**            | ❌ 未实现 | 需新增FollowUpRecommendation组件 |

### 4.3 技术架构

```
Agent执行流程（增强版）:
1. 用户输入 → Agent.solve() → 获得答案
2. [新增] is_math_problem() → 判断是否触发推荐
3. [新增] FollowUpRecommender.recommend() → 推荐2道题
4. [新增] format_follow_up_text() → 格式化为Markdown
5. [新增] SSE event: "follow_up" → 流式追加到前端
6. [新增] 前端 FollowUpRecommendation 组件 → 渲染推荐卡片
```

### 4.4 核心实现

#### 4.4.1 Agent集成

**修改文件**: `agent_core/agent.py`

```python
async def solve(self, input_text, user_id="anonymous",
                session_id="default", images=None):
    start_time = time.time()

    # Step 1: 执行推理
    context = AgentContext(
        input=input_text, user_id=user_id,
        session_id=session_id, images=images,
    )
    strategy = self._select_strategy(context)
    result = await strategy.execute(context)

    # Step 2: [新增] 跟进推荐
    follow_up_text = ""
    if self._should_recommend(input_text, result):
        follow_up_text = await self._generate_follow_up(
            user_input=input_text, user_id=user_id,
        )

    # Step 3: 构建响应
    response = {
        "answer": result.answer,
        "thoughts": result.thoughts,
        "metadata": result.metadata,
        "execution_time": time.time() - start_time,
    }
    if follow_up_text:
        response["follow_up"] = follow_up_text
        response["answer"] += "\n" + follow_up_text
    return response

def _should_recommend(self, input_text, result) -> bool:
    """判断是否触发推荐"""
    from app.services.follow_up_recommender import is_math_problem
    if not is_math_problem(input_text):
        return False
    if not result.answer or len(result.answer.strip()) < 10:
        return False
    return True

async def _generate_follow_up(self, user_input, user_id) -> str:
    """生成推荐内容（失败返回空字符串）"""
    try:
        from app.services.follow_up_recommender import (
            get_follow_up_recommender, format_follow_up_text,
        )
        recommender = get_follow_up_recommender()
        result = await recommender.recommend(
            user_input=user_input, user_id=user_id,
        )
        if result.questions:
            return format_follow_up_text(result)
    except Exception as e:
        logger.warning(f"跟进推荐失败（已忽略）: {e}")
    return ""
```

#### 4.4.2 SSE流式追加

**修改文件**: `app/api/v1/endpoints/chat.py`

```python
@router.post("/chat/stream")
async def chat_stream(request, current_user=Depends(get_current_user)):
    async def event_generator():
        # Step 1: 流式输出Agent回答
        async for chunk in agent.stream(
            request.message, user_id=current_user.id
        ):
            yield f"event: message\ndata: {json.dumps({'content': chunk})}\n\n"

        # Step 2: [新增] 推荐内容
        follow_up = getattr(agent, '_follow_up_text', None)
        if follow_up:
            yield (
                f"event: follow_up\ndata: "
                f"{json.dumps({'type': 'recommendation', 'content': follow_up})}\n\n"
            )

        # Step 3: 结束
        yield f"event: done\ndata: {json.dumps({'status': 'complete'})}\n\n"

    return StreamingResponse(event_generator(), media_type="text/event-stream")
```

#### 4.4.3 前端推荐组件

**新建文件**: `frontend/src/components/FollowUpRecommendation.vue`

```vue
<template>
  <div class="follow-up-card" v-if="questions.length">
    <div class="header">
      <span class="title">推荐练习</span>
    </div>
    <div class="questions">
      <div
        v-for="(q, i) in questions"
        :key="q.id"
        class="question-item"
        @click="$emit('select', q)"
      >
        <div class="question-header">
          <span class="label">{{ i === 0 ? '基础巩固' : '能力提升' }}</span>
          <span class="difficulty">难度 {{ q.difficulty }}/5</span>
        </div>
        <div class="question-content" v-html="renderMarkdown(q.content)"></div>
        <details class="answer-section">
          <summary>查看答案</summary>
          <div v-html="renderMarkdown(q.answer)"></div>
        </details>
      </div>
    </div>
  </div>
</template>

<script setup>
defineProps({ questions: { type: Array, default: () => [] } })
defineEmits(['select'])

const renderMarkdown = (text) => {
  return window.mdRenderer?.render(text) || text
}
</script>

<style scoped>
.follow-up-card {
  margin: 16px 0;
  border: 1px solid #e0e0e0;
  border-radius: 12px;
  padding: 16px;
  background: #fafafa;
}
.header { margin-bottom: 12px; font-weight: 600; }
.question-item {
  padding: 12px;
  margin-bottom: 8px;
  border-radius: 8px;
  background: white;
  cursor: pointer;
  transition: box-shadow 0.2s;
}
.question-item:hover { box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
.question-header { display: flex; justify-content: space-between; margin-bottom: 8px; }
.label { font-weight: 500; color: #333; }
.difficulty { font-size: 0.85em; color: #666; }
</style>
```

### 4.5 推荐流程时序

```
用户          Agent          FollowUpRecommender      数据库         LLM
 |--输入数学题->|                    |                   |             |
 |             |--推理+回答-------->|                   |             |
 |             |--is_math_problem?->|                   |             |
 |             |<---true-----------|                   |             |
 |             |--recommend()------>|                   |             |
 |             |                    |--提取知识点------->|             |
 |             |                    |<--category/kp----|             |
 |             |                    |--计算双难度------->|             |
 |             |                    |<--Q1=2,Q2=4-----|             |
 |             |                    |--检索候选题------->|             |
 |             |                    |<--2道题----------|             |
 |             |<--FollowUpResult--|                   |             |
 |<--SSE:follow_up-----------------|                   |             |
```

### 4.6 降级策略

| 场景                    | 降级方案         |
| --------------------- | ------------ |
| FollowUpRecommender异常 | 静默忽略，不追加推荐   |
| 题库检索不足2题              | 跨类别兜底检索      |
| 题库+跨类别仍不足             | LLM兜底生成原创题   |
| LLM也失败                | 返回空推荐，不影响主回答 |

### 4.7 测试方案

```python
@pytest.mark.asyncio
async def test_follow_up_in_agent():
    agent = MathAgent(MathAgentConfig())
    result = await agent.solve("求 f(x)=x^2 的导数", user_id="test")
    assert "follow_up" in result
    assert "推荐练习" in result["follow_up"]

@pytest.mark.asyncio
async def test_no_follow_up_for_concept():
    agent = MathAgent(MathAgentConfig())
    result = await agent.solve("什么是导数？", user_id="test")
    assert "follow_up" not in result

@pytest.mark.asyncio
async def test_follow_up_failure_graceful():
    agent = MathAgent(MathAgentConfig())
    with patch('...FollowUpRecommender.recommend', side_effect=Exception("mock")):
        result = await agent.solve("求 f(x)=x^2 的导数", user_id="test")
        assert result["answer"]  # 主回答正常
```

### 4.8 验收标准

- [x] Agent.solve() 自动追加推荐内容
- [x] 推荐2道难度递增的练习题
- [x] SSE流式推送 `follow_up` 事件
- [x] 前端正确渲染推荐卡片（FollowUpRecommendation 组件）
- [x] 推荐失败不影响主流程
- [x] 单元测试覆盖率 >= 80%（8个测试用例全部通过）

***

## 附录

### A. 关键文件清单

| 文件                                                   | 操作  | 说明               |
| ---------------------------------------------------- | --- | ---------------- |
| `agent_core/memory_extractor.py`                     | 新建  | 自动记忆提取器          |
| `agent_core/memory_persistence.py`                   | 修改  | 新增批量写入           |
| `agent_core/strategies/langchain_react_strategy.py`  | 修改  | 集成记忆提取           |
| `agent_core/agent.py`                                | 修改  | 集成跟进推荐           |
| `app/api/v1/endpoints/chat.py`                       | 修改  | SSE follow\_up事件 |
| `app/services/follow_up_recommender.py`              | 无修改 | 已实现              |
| `frontend/src/components/FollowUpRecommendation.vue` | 新建  | 推荐卡片组件           |

### B. 开发排期

| 阶段      | 任务                              | 工时   | 依赖    |
| ------- | ------------------------------- | ---- | ----- |
| Day 1-2 | P0-01: memory\_extractor + 策略集成 | 1.5天 | 无     |
| Day 2-3 | P0-02: 批量写入 + 重试机制              | 2天   | P0-01 |
| Day 4-5 | P0-03: Agent集成 + SSE + 前端       | 1.5天 | P0-01 |
| Day 6   | 集成测试 + 性能测试                     | 0.5天 | 全部    |
| Day 7   | 文档 + Code Review                | 0.5天 | 全部    |

### C. 风险矩阵

| 风险        | 概率 | 影响 | 缓解             |
| --------- | -- | -- | -------------- |
| 记忆提取器性能问题 | 中  | 高  | 异步执行，5秒超时跳过    |
| 批量写入失败    | 低  | 中  | 指数退避重试3次       |
| 推荐内容质量差   | 中  | 中  | LLM兜底 + 人工审核题库 |
| SSE连接不稳定  | 低  | 低  | 前端自动重连         |
| 题库数据不足    | 中  | 高  | 优先补充高频知识点      |

### D. 参考资料

- [PRD.md](../prd/PRD.md)
- [技术架构全面评估与优化方案.md](../architecture/技术架构全面评估与优化方案.md)
- [第二阶段\_交互功能增强实施方案.md](./第二阶段_交互功能增强实施方案.md)

