# A03：自动记忆持久化机制 — AI 可执行开发规格书 v2.2

> **最后更新**：2026-05-30  
> **规格等级**：AI 可直接开发（含 DDL / 精确行号 / 接口签名 / 验收脚本）  
> **开发顺序**：T0 → T1 → T2 → (T3 ∥ T4a) → T4b → T4c → T5 → T6 → T7 → T8

---

## 文档结构说明

本规格书分为六个部分，按 AI 开发者的阅读顺序排列：

| 章节 | 内容 | 用途 |
|---|---|---|
| **第〇章** | 前置准备：DDL、文件清单、不可破坏契约 | 开发环境就绪 |
| **第一章** | 现状评估与架构决策 | 理解上下文 |
| **第二章** | 任务总览与依赖图 | 全局导航 |
| **第三章** | T0 - T8 任务完整规格（精确到行号） | **直接编码** |
| **第四章** | Skill 追加机制（T4a / T4b / T4c） | **直接编码** |
| **第五章** | 附录：验收检查脚本、Skill DAG YAML、Git 规范 | 验证交付 |

---

# 第〇章：开发前置准备

## 0.1 数据库迁移（必须首先执行）

### 文件：`app/data/migrations/003_add_user_skills.sql`

```sql
-- ============================================================
-- A03 用户技能熟练度表
-- 必须在启动任何 T1-T8 开发前执行
-- ============================================================

CREATE TABLE IF NOT EXISTS user_skills (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    skill_code VARCHAR(50) NOT NULL,

    display_name VARCHAR(100),
    category_path VARCHAR(200),

    mastery_level FLOAT NOT NULL DEFAULT 0.0
        CHECK (mastery_level >= 0.0 AND mastery_level <= 1.0),

    status VARCHAR(20) NOT NULL DEFAULT 'novice'
        CHECK (status IN ('novice', 'learning', 'proficient', 'mastered')),

    total_attempts INT NOT NULL DEFAULT 0,
    correct_count INT NOT NULL DEFAULT 0,
    recent_streak INT NOT NULL DEFAULT 0,
    best_streak INT NOT NULL DEFAULT 0,

    first_seen_at TIMESTAMPTZ,
    last_practiced_at TIMESTAMPTZ,
    mastered_at TIMESTAMPTZ,

    evolution_history JSONB NOT NULL DEFAULT '[]',

    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),

    UNIQUE (user_id, skill_code)
);

CREATE INDEX IF NOT EXISTS idx_us_user_mastery
    ON user_skills (user_id, mastery_level DESC);

CREATE INDEX IF NOT EXISTS idx_us_user_status
    ON user_skills (user_id, status);

CREATE INDEX IF NOT EXISTS idx_us_skill_code
    ON user_skills (skill_code);
```

### 执行方式

```bash
# PostgreSQL:
psql -U <user> -d <db> -f app/data/migrations/003_add_user_skills.sql

# SQLite（开发环境）:
sqlite3 data/math_ai.db < app/data/migrations/003_add_user_skills.sql
```

### 新 Django ORM 模型同步（若使用）

`app/data/models.py:L98` 之后新增：

```python
class UserSkill(Base):
    """用户技能熟练度表（A03 Skill机制）"""
    __tablename__ = "user_skills"

    id = Column(Integer, primary_key=True, autoincrement=True)
    user_id = Column(
        String(36), ForeignKey("users.id"), nullable=False, index=True
    )
    skill_code = Column(String(50), nullable=False)
    display_name = Column(String(100))
    category_path = Column(String(200))

    mastery_level = Column(Float, nullable=False, default=0.0)
    status = Column(String(20), nullable=False, default="novice")

    total_attempts = Column(Integer, nullable=False, default=0)
    correct_count = Column(Integer, nullable=False, default=0)
    recent_streak = Column(Integer, nullable=False, default=0)
    best_streak = Column(Integer, nullable=False, default=0)

    first_seen_at = Column(DateTime(timezone=True))
    last_practiced_at = Column(DateTime(timezone=True))
    mastered_at = Column(DateTime(timezone=True))

    evolution_history = Column(JSON, default=list)

    created_at = Column(
        DateTime(timezone=True),
        default=lambda: datetime.now(timezone.utc),
    )
    updated_at = Column(
        DateTime(timezone=True),
        default=lambda: datetime.now(timezone.utc),
        onupdate=lambda: datetime.now(timezone.utc),
    )

    __table_args__ = (
        Index("idx_us_user_mastery", "user_id", mastery_level.desc()),
        Index("idx_us_user_status", "user_id", "status"),
        Index("idx_us_skill_code", "skill_code"),
        UniqueConstraint("user_id", "skill_code", name="uq_user_skill"),
    )
```

---

## 0.2 新建 / 修改文件清单

```
需要修改的现有文件：
├── agent_core/agent.py              ← 主要集成点
├── app/services/memory.py           ← 扩展 get_user_profile()
├── app/data/models.py               ← 新增 UserSkill ORM（见 0.1）
├── app/data/database.py             ← init_db() 自动建表（含 user_skills）
└── tests/                           ← 新增测试文件（不修改现有测试）

需要新建的文件：
├── agent_core/memory_persistence.py   ← T1: MemoryPersistenceFacade
├── app/services/event_buffer.py       ← T3: EnhancedEventBuffer
├── app/services/skill_aggregator.py   ← T4a: SkillAggregator
├── app/services/difficulty_estimator.py ← T4c: DifficultyEstimator
├── app/services/math_skill_dag.py     ← T4b: MathSkillDAG
├── app/data/math_skill_graph.yml      ← T4b: 静态知识依赖图
├── app/data/migrations/003_add_user_skills.sql ← 见 0.1
└── tests/test_memory_persistence.py   ← T7: 专项测试
```

---

## 0.3 不可破坏契约（AI 必须严格遵守）

### 绝对不能修改的部分

| 文件 | 行号范围 | 内容 | 理由 |
|---|---|---|---|
| `agent_core/context_manager.py` | L51-L56 | `DEFAULT_*` 常量 | 128K 预算分配基线 |
| `agent_core/context_manager.py` | L870-L949 | `build_llm_context()` 签名 | Agent 策略依赖 |
| `app/api/memory_api.py` | L56-L175 | `POST /events` + `GET /retrieve` | 现有 API 契约 |
| `app/data/models.py` | L58-L96 | `LearningRecord` 字段 | 后端依赖 |
| `agent_core/agent.py` | L570-L631 | `process()` / `stream()` 主流程签名 | 上层调用方 |
| `agent_core/agent.py` | L339 | `_build_context()` 签名 | 策略依赖 |

### 只能扩展、不能删除或重命名的接口

| 文件 | 方法 | 约束 |
|---|---|---|
| `app/services/memory.py:L139` | `get_user_profile()` | 返回值可**加字段**，不可删减原有字段 |
| `app/services/memory.py:L247` | `get_relevant_memories()` | 签名不变 |
| `app/services/memory.py:L340` | `record_learning_event()` | 签名不变 |
| `agent_core/agent.py:L109-L237` | `MathAgent.__init__()` | 只能**在尾部加新属性**，不可重排参数 |

### 依赖注入规则

所有新模块必须通过以下方式注入 Agent：
```
MathAgent.__init__
    → self._persistence_facade = MemoryPersistenceFacade(db_session_factory)
    → _select_strategy() 内将 facade 引用传递给 strategy
```

不可使用全局单例或模块级导入。

---

## 0.4 Python 导入约定

```python
# 所有新增文件使用统一导入风格：

from __future__ import annotations
import logging
from typing import Any, Dict, List, Optional, Tuple
from dataclasses import dataclass, field
from datetime import datetime, timezone

logger = logging.getLogger(__name__)
```

---

# 第一章：现状评估与架构决策

## 1.1 代码库实现状态

| 模块 | 文件 | 状态 | 说明 |
|---|---|---|---|
| ShortTermMemory | `app/services/memory.py:L41-L132` | ✅ | 会话内检索 |
| LongTermMemory | `app/services/memory.py:L135-L370` | ✅ | 数据库 CRUD |
| MemoryRetrievalEngine | `app/services/memory.py:L381-L483` | ✅ | 双路检索 |
| Memory API | `app/api/memory_api.py` | ✅ | POST `/events` + GET `/retrieve` |
| SmartContextManager | `agent_core/context_manager.py:L672` | ✅ | 128K 上下文管理 |
| 128K 集成 | `agent_core/agent.py:L152-L165, L339-L450` | ✅ | `build_llm_context` |
| response 持久化 | `agent_core/agent.py:L518-L563` | ✅ | `save_assistant_response_to_context` |
| **统一入口** | 无 | ❌ | T1 新增 `MemoryPersistenceFacade` |
| **LLM 分类** | 无 | ❌ | T2 新增 |
| **事件缓冲** | 无 | ❌ | T3 新增 `EnhancedEventBuffer` |
| **Skill 熟练度** | 无 | ❌ | T4a 新增 |
| **知识依赖图** | 无 | ❌ | T4b 新增 |
| **难度估算** | 仅基于正确率 | ❌ | T4c 升级 |
| **Intent 映射** | 无 | ❌ | T5 新增 |
| **Prometheus** | 无 | ❌ | T6 新增 |
| **专项测试** | 无 | ❌ | T7 新增 |

## 1.2 架构决策

**维持现有分层架构**（`app/services/memory.py` + `agent_core/context_manager.py`），不创建独立的 `agent_core/memory_persist.py` 替代方案。

新增 `MemoryPersistenceFacade` 作为统一入口，委托到已有模块，不做破坏性修改。

---

# 第二章：任务总览

| 编号 | 任务 | 优先级 | 预估 | 依赖 | 新建文件数 |
|---|---|---|---|---|---|
| **T0** | 前置准备（DDL + ORM） | **P0** | 0.5d | 无 | 2 |
| **T1** | MemoryPersistenceFacade | **P0** | 1d | T0 | 1 |
| **T2** | LLM 事件分类 | **P0** | 2d | T1 | 0 |
| **T3** | EnhancedEventBuffer | P1 | 2d | T1 | 1 |
| **T4a** | SkillAggregator | P1 | 1.5d | T1 | 1 |
| **T4b** | MathSkillDAG | P1 | 1d | T4a | 2 |
| **T4c** | DifficultyEstimator | P1 | 2d | T4a | 1 |
| **T5** | Intent → 持久化映射 | P1 | 1d | T2 | 0 |
| **T6** | Prometheus 指标 | P1 | 1.5d | T1 | 0 |
| **T7** | 专项测试 | P1 | 2d | T2-T6 | 1 |
| **T8** | Runbook 文档 | P2 | 0.5d | T6 | 0 |

### 依赖图

```
T0 ──→ T1 ──→ T2 ──→ T4c
          │      │
          │      └──→ T5
          │
          ├──→ T3
          ├──→ T4a ──→ T4b
          │           └──→ T4c
          │
          └──→ T6 ──→ T8

T2-T6 ──→ T7
```

### 时间线

```
Week 1: T0 → T1 → T2
Week 2: T3 ∥ T4a → T4b → T4c
Week 3: T5 → T6 → T8
Week 4: T7（全量测试）
```

---

# 第三章：任务完整规格

---

## T0：前置准备（0.5d）

### 3.0.1 目标

创建数据库表 + ORM 模型 + Skill DAG 数据文件，为 T1-T8 提供数据基础。

### 3.0.2 精确操作

**操作 1**：创建 `app/data/migrations/003_add_user_skills.sql`（内容见 §0.1）。

**操作 2**：在 `app/data/database.py:L79-L80`（`await conn.run_sync(Base.metadata.create_all)`）之后，添加原始 SQL 执行：

```python
# app/data/database.py L81 之后
# 新增：
try:
    import os
    migration_path = os.path.join(
        os.path.dirname(__file__), "migrations", "003_add_user_skills.sql"
    )
    if os.path.exists(migration_path):
        with open(migration_path, "r") as f:
            migration_sql = f.read()
        await conn.run_sync(
            lambda sync_conn: sync_conn.execute(
                __import__("sqlalchemy").text(migration_sql)
            )
        )
    logger.info("A03 user_skills 表迁移检查完成")
except Exception as e:
    logger.warning(f"A03 迁移执行异常（表可能已存在）: {e}")
```

**操作 3**：在 `app/data/models.py:L98` 之后新增 `UserSkill` ORM（内容见 §0.1）。

**操作 4**：创建 `app/data/math_skill_graph.yml`：

```yaml
# 数学技能依赖图（高中数学核心部分）
# 格式: skill_code → {display_name, prerequisites, unlocks, category}

# ======== 代数基础 ========
basic_operations:
  display_name: "基本运算"
  prerequisites: []
  category: "代数"

linear_equations:
  display_name: "一元一次方程"
  prerequisites: ["basic_operations"]
  category: "代数"

quadratic_equations:
  display_name: "一元二次方程"
  prerequisites: ["linear_equations"]
  category: "代数"

polynomials:
  display_name: "多项式"
  prerequisites: ["quadratic_equations"]
  category: "代数"

# ======== 函数 ========
function_concept:
  display_name: "函数概念"
  prerequisites: ["basic_operations"]
  category: "函数"

elementary_functions:
  display_name: "初等函数"
  prerequisites: ["function_concept"]
  category: "函数"

composite_functions:
  display_name: "复合函数"
  prerequisites: ["elementary_functions"]
  category: "函数"

# ======== 三角函数 ========
trig_basic_values:
  display_name: "三角基本值"
  prerequisites: ["elementary_functions"]
  category: "三角函数"

trig_identities:
  display_name: "三角恒等式"
  prerequisites: ["trig_basic_values"]
  category: "三角函数"

trig_sum_formula:
  display_name: "和角公式"
  prerequisites: ["trig_identities"]
  category: "三角函数"

trig_double_angle:
  display_name: "倍角公式"
  prerequisites: ["trig_sum_formula"]
  category: "三角函数"

trig_equation:
  display_name: "解三角形"
  prerequisites: ["trig_sum_formula", "trig_double_angle"]
  category: "三角函数"

# ======== 极限与导数 ========
limit_concept:
  display_name: "极限概念"
  prerequisites: ["function_concept"]
  category: "极限"

limit_calculation:
  display_name: "极限计算"
  prerequisites: ["limit_concept", "polynomials"]
  category: "极限"

derivative_definition:
  display_name: "导数定义"
  prerequisites: ["limit_calculation"]
  category: "导数"

derivative_rules:
  display_name: "求导法则"
  prerequisites: ["derivative_definition"]
  category: "导数"

derivative_application:
  display_name: "导数应用（切线/极值）"
  prerequisites: ["derivative_rules"]
  category: "导数"

integ_indefinite:
  display_name: "不定积分"
  prerequisites: ["derivative_rules"]
  category: "积分"

integ_definite:
  display_name: "定积分"
  prerequisites: ["integ_indefinite", "derivative_application"]
  category: "积分"

integ_by_parts:
  display_name: "分部积分法"
  prerequisites: ["integ_indefinite"]
  category: "积分"

integ_trig:
  display_name: "三角积分"
  prerequisites: ["integ_indefinite", "trig_double_angle"]
  category: "积分"
```

### 3.0.3 验收

```bash
python -c "
from app.data.database import init_db
import asyncio
asyncio.run(init_db())
from app.data.models import UserSkill
print('T0 OK: UserSkill model loaded')
"
```

---

## T1：MemoryPersistenceFacade 统一入口（1d）

### 3.1.1 目标

创建统一的记忆持久化入口，供 `MathAgent` 的三个策略使用。

### 3.1.2 新建文件 `agent_core/memory_persistence.py`

**完整接口签名和实现骨架**：

```python
from __future__ import annotations

import json
import logging
from dataclasses import dataclass, field
from datetime import datetime, timezone
from typing import Any, Dict, List, Optional

from app.services.memory import (
    LongTermMemory,
    MemoryRetrievalEngine,
    ShortTermMemory,
    MemoryItem,
)
from app.data.database import get_db_session

logger = logging.getLogger(__name__)


@dataclass
class UserProfile:
    """用户画像（扩展版），包含原有统计字段 + Skill 字段 + 错误模式"""

    # ====== 原有字段（兼容 LongTermMemory.get_user_profile 输出）======
    total_questions: int = 0
    correct_rate: float = 0.0
    avg_time_per_question: float = 0.0
    weak_points: List[Dict[str, Any]] = field(default_factory=list)
    strong_points: List[str] = field(default_factory=list)
    recommended_difficulty: int = 3
    recent_activity: List[Dict[str, Any]] = field(default_factory=list)

    # ====== A03 新增: Skill 字段 ======
    skills: List[Dict[str, Any]] = field(default_factory=list)

    # ====== A03 新增: 错误模式关联 ======
    error_patterns: List[Dict[str, Any]] = field(default_factory=list)

    # ====== A03 新增: 认知偏好（Hermes 风格）======
    cognitive_style: Dict[str, Any] = field(default_factory=dict)

    def to_dict(self) -> Dict[str, Any]:
        return {
            "total_questions": self.total_questions,
            "correct_rate": self.correct_rate,
            "avg_time_per_question": self.avg_time_per_question,
            "weak_points": self.weak_points,
            "strong_points": self.strong_points,
            "recommended_difficulty": self.recommended_difficulty,
            "recent_activity": self.recent_activity,
            "skills": self.skills,
            "error_patterns": self.error_patterns,
            "cognitive_style": self.cognitive_style,
        }

    def to_compact_json(self, max_length: int = 800) -> str:
        """输出 Hermes 风格的紧凑 JSON（不超过 max_length 字符）"""
        compact = {
            "cr": self.correct_rate,
            "wp": [
                {"c": w["category"], "m": w["mastery"]}
                for w in self.weak_points
            ],
            "s": [
                {
                    "c": s["skill_code"],
                    "m": s["mastery_level"],
                    "st": s["status"][0],
                }
                for s in self.skills
            ],
            "ep": [
                {"p": e["pattern"], "f": e["frequency"]}
                for e in self.error_patterns
            ],
        }
        text = json.dumps(compact, ensure_ascii=False, separators=(",", ":"))
        if len(text) > max_length:
            # 裁剪: 保留前 N 个 skill
            while len(text) > max_length and compact.get("s"):
                compact["s"].pop()
                text = json.dumps(
                    compact, ensure_ascii=False, separators=(",", ":")
                )
        return text


class MemoryPersistenceFacade:
    """
    记忆持久化统一入口（Facade 模式）。

    委托到 LongTermMemory、MemoryRetrievalEngine、SkillAggregator（T4a）。
    所有方法均为 async，支持在 Agent 策略中直接调用。
    """

    def __init__(self, db_session_factory=None):
        self._session_factory = db_session_factory or get_db_session
        self._long_term = LongTermMemory(self._session_factory)
        self._skill_aggregator = None  # T4a 之后注入
        self._difficulty_estimator = None  # T4c 之后注入
        logger.info("MemoryPersistenceFacade 初始化完成")

    async def record_event(
        self, user_id: str, event_data: Dict[str, Any]
    ) -> bool:
        """记录一次学习事件，委托到 LongTermMemory.record_learning_event。

        Args:
            user_id: 用户 ID
            event_data: 事件数据字典，必须符合 LearningRecord 字段
                必填: event_type, question_content, category
                可选: sub_categories, difficulty, is_correct,
                      user_answer, correct_answer, time_spent,
                      error_category, error_reason, metadata

        Returns:
            bool: True 表示写入成功

        Raises:
            无：内部捕获异常并记录日志，返回 False
        """
        event_data["user_id"] = user_id
        return await self._long_term.record_learning_event(event_data)

    async def retrieve_context(
        self,
        user_id: str,
        query: str,
        session_id: str = "default",
        limit: int = 10,
        min_score: float = 0.3,
    ) -> List[Dict[str, Any]]:
        """检索与当前查询相关的历史记忆。

        Args:
            user_id: 用户 ID
            query: 搜索查询文本
            session_id: 会话 ID（用于短期记忆）
            limit: 返回结果上限
            min_score: 最低相关性分数

        Returns:
            List[Dict]: 格式 [{"id":...,"type":...,"content":...,"relevance":...,"source":...}, ...]
        """
        short_term = ShortTermMemory()
        engine = MemoryRetrievalEngine(
            short_term_memory=short_term,
            long_term_memory=self._long_term,
        )
        results = await engine.retrieve(
            query=query, user_id=user_id, top_k=limit, min_score=min_score
        )
        return [
            {
                "id": r.memory.id,
                "type": r.memory.memory_type,
                "content": r.memory.content,
                "relevance": r.score,
                "source": r.source,
                "explanation": r.explanation,
            }
            for r in results
        ]

    async def get_profile(self, user_id: str) -> UserProfile:
        """获取扩展用户画像（含 Skill + Error Pattern）。

        Args:
            user_id: 用户 ID

        Returns:
            UserProfile: 包含所有维度的完整画像
        """
        # 获取基础统计（现有 LongTermMemory 兼容）
        base = await self._long_term.get_user_profile(user_id)

        profile = UserProfile(
            total_questions=base.get("total_questions", 0),
            correct_rate=base.get("correct_rate", 0.0),
            avg_time_per_question=base.get("avg_time_per_question", 0.0),
            weak_points=base.get("weak_points", []),
            strong_points=base.get("strong_points", []),
            recommended_difficulty=base.get("recommended_difficulty", 3),
            recent_activity=base.get("recent_activity", []),
        )

        # T4a 完成后：填充 skills / error_patterns / cognitive_style
        if self._skill_aggregator:
            profile.skills = await self._skill_aggregator.get_all_skills(
                user_id
            )
            profile.error_patterns = (
                await self._skill_aggregator.get_error_patterns(user_id)
            )
            profile.cognitive_style = (
                await self._skill_aggregator.get_cognitive_style(user_id)
            )

        return profile
```

### 3.1.3 集成到 `agent_core/agent.py`（精确行号）

**修改 1**：`agent.py:L15-L25` import 区域（追加一行）：

```python
# agent.py L25 之后新增：
from agent_core.memory_persistence import MemoryPersistenceFacade, UserProfile
```

**修改 2**：`agent.py:L231-L237` log 之前，新增实例化：

```python
# agent.py L231 之前插入：
# ── 🆕 A03: 记忆持久化统一入口 ──
self._persistence_facade = MemoryPersistenceFacade()
logger.info("MemoryPersistenceFacade 已初始化")
```

**修改 3**：`agent.py:L586-L590` `save_assistant_response_to_context(...)` 之后追加事件记录：

```python
# agent.py L590 之后新增：
if self._persistence_facade and user_id:
    try:
        await self._persistence_facade.record_event(
            user_id=user_id,
            event_data={
                "event_type": "problem_solving",
                "question_content": user_input[:500],
                "category": "综合",
                "metadata": {
                    "response_length": len(result),
                    "strategy": type(strategy).__name__,
                },
            },
        )
    except Exception as e:
        logger.warning(f"记忆持久化记录失败（非致命）: {e}")
```

**修改 4**：`agent.py:L621-L630` 同样的追加（`stream()` 方法末尾）：

```python
# agent.py L629 metadata 行之后新增：
if self._persistence_facade and user_id:
    try:
        await self._persistence_facade.record_event(
            user_id=user_id,
            event_data={
                "event_type": "problem_solving",
                "question_content": user_input[:500],
                "category": "综合",
                "metadata": {
                    "response_length": len(full_response),
                    "strategy": type(strategy).__name__,
                },
            },
        )
    except Exception as e:
        logger.warning(f"流式记忆持久化记录失败（非致命）: {e}")
```

**注**：`user_id` 变量当前未传入 `process()` 和 `stream()`。需要在这两个方法签名中增加可选参数 `user_id: Optional[str] = None`（`L570`, `L594`），并在 `_build_context` 调用时传递。此修改向后兼容（默认 `None` 时跳过持久化）。

### 3.1.4 `process()` 和 `stream()` 签名修改

```python
# agent.py L570 修改为：
async def process(
    self,
    user_input: str,
    session_id: Optional[str] = None,
    user_id: Optional[str] = None,  # 🆕 新增
) -> str:

# agent.py L594 修改为：
async def stream(
    self,
    user_input: str,
    session_id: Optional[str] = None,
    user_id: Optional[str] = None,  # 🆕 新增
) -> AsyncGenerator[str, None]:
```

### 3.1.5 验收检查

```bash
python -c "
import asyncio
from agent_core.memory_persistence import MemoryPersistenceFacade, UserProfile

async def test():
    facade = MemoryPersistenceFacade()
    # 测试 1: record_event
    ok = await facade.record_event('test_user', {
        'event_type': 'answer_correct',
        'question_content': '求 sin(30°) 的值',
        'category': '三角函数',
        'is_correct': True,
    })
    assert ok, 'record_event failed'
    print('PASS: record_event')

    # 测试 2: get_profile (基础字段)
    profile = await facade.get_profile('test_user')
    assert isinstance(profile, UserProfile)
    assert 'total_questions' in profile.to_dict()
    print(f'PASS: get_profile → {profile.to_dict()}')

    # 测试 3: to_compact_json 不超 1000 字符
    compact = profile.to_compact_json(max_length=1000)
    assert len(compact) <= 1000
    print(f'PASS: to_compact_json length={len(compact)}')

asyncio.run(test())
"

# 如果数据库未初始化，先运行:
# python -c "import asyncio; from app.data.database import init_db; asyncio.run(init_db())"
```

---

## T2：LLM 事件分类（2d）

### 3.2.1 目标

替换 `LongTermMemory._extract_categories()`（`app/services/memory.py:L354`）的纯关键词匹配，增加 LLM 分类能力。

### 3.2.2 修改文件 `app/services/memory.py`

**修改前**（L354-L370）——保留 `_extract_categories` 作为降级路径，不要删除。

**新增**：在 `LongTermMemory` 类中追加以下方法（L371 之后）：

```python
    async def _classify_event(
        self, event_data: Dict[str, Any], llm=None
    ) -> EventClassification:
        """
        使用 LLM 对学习事件进行分类。

        降级策略: LLM 失败 → _extract_categories 关键词匹配。

        Args:
            event_data: 事件数据字典
            llm: 可选的 LLM 实例（未提供时降级到关键词）

        Returns:
            EventClassification: 分类结果
        """
        if llm is None:
            return self._classify_fallback(event_data)

        prompt = (
            "Classify this math learning event into a JSON object. "
            "Categories: 极限, 导数, 积分, 三角函数, 代数, 解析几何, 概率统计, 向量, 数列.\n\n"
            f"Content: {event_data.get('question_content', '')[:200]}\n"
            f"Tags hint: {event_data.get('sub_categories', '')}\n\n"
            'Return ONLY: {"event_type":"question|answer|clarification|error_correction|review",'
            '"category":"<one of above>","difficulty":<1-5>,"importance":<0.0-1.0>,'
            '"is_milestone":<true|false>,"tags":["tag1","tag2"]}'
        )

        try:
            from langchain_core.messages import HumanMessage
            response = await llm.ainvoke([HumanMessage(content=prompt)])
            text = response.content.strip()

            import json
            import re
            match = re.search(r"\{.*\}", text, re.DOTALL)
            if match:
                data = json.loads(match.group())
                return EventClassification(
                    event_type=data.get("event_type", "question"),
                    category=data.get("category", ""),
                    difficulty=int(data.get("difficulty", 3)),
                    importance=float(data.get("importance", 0.5)),
                    is_milestone=data.get("is_milestone", False),
                    tags=data.get("tags", []),
                )
        except Exception as e:
            logger.warning(f"LLM 事件分类失败，降级到关键词匹配: {e}")

        return self._classify_fallback(event_data)

    def _classify_fallback(
        self, event_data: Dict[str, Any]
    ) -> EventClassification:
        """关键词匹配降级路径"""
        text = (
            event_data.get("question_content", "")
            + " "
            + event_data.get("sub_categories", "")
        )
        categories = self._extract_categories(text)

        return EventClassification(
            event_type=(
                "answer"
                if event_data.get("is_correct") is not None
                else "question"
            ),
            category=categories[0] if categories else "",
            difficulty=event_data.get("difficulty", 3),
            importance=0.5,
            is_milestone=False,
            tags=categories,
        )
```

**新增 dataclass**：`app/services/memory.py:L10` 之后插入：

```python
from dataclasses import dataclass, field


@dataclass
class EventClassification:
    """LLM 分类结果"""
    event_type: str = "question"
    category: str = ""
    difficulty: int = 3
    importance: float = 0.5
    is_milestone: bool = False
    tags: List[str] = field(default_factory=list)
```

### 3.2.3 集成到 `MemoryPersistenceFacade`

在 `agent_core/memory_persistence.py` 的 `record_event` 方法中，增加 LLM 分类前置步骤：

```python
async def record_event(
    self, user_id: str, event_data: Dict[str, Any],
    classify_with_llm: bool = False,
    llm_instance=None,
) -> bool:
    event_data["user_id"] = user_id

    if classify_with_llm and llm_instance:
        classification = await self._long_term._classify_event(
            event_data, llm=llm_instance
        )
        event_data["category"] = classification.category
        event_data["difficulty"] = classification.difficulty

    return await self._long_term.record_learning_event(event_data)
```

### 3.2.4 验收

```python
# 验证 LLM 分类 + 关键词降级 两条路径
from app.services.memory import LongTermMemory, EventClassification
from app.data.database import get_db_session

ltm = LongTermMemory(get_db_session)

# 路径 1: 关键词降级
result = ltm._classify_fallback({
    "question_content": "求极限 lim(x→0) sinx/x",
    "is_correct": True,
})
assert result.category == "极限", f"Expected 极限, got {result.category}"

# 路径 2: LLM 分类（需要 LLM 实例）
# result = await ltm._classify_event(event_data, llm=my_llm)

print("T2 OK")
```

---

## T3：EnhancedEventBuffer（2d）

### 3.3.1 目标

在内存中缓冲学习事件，满足触发条件时批量写入数据库，减少写入次数。

### 3.3.2 新建文件 `app/services/event_buffer.py`

```python
from __future__ import annotations

import asyncio
import hashlib
import logging
import time
from collections import deque
from dataclasses import dataclass, field
from datetime import datetime, timezone
from typing import Any, Dict, List, Optional

logger = logging.getLogger(__name__)


@dataclass
class BufferedEvent:
    """缓冲区中的事件"""
    user_id: str
    event_type: str
    question_content: str
    category: str
    data: Dict[str, Any]
    content_hash: str = ""
    created_at: float = field(default_factory=time.time)

    def __post_init__(self):
        if not self.content_hash:
            self.content_hash = hashlib.md5(
                f"{self.user_id}:{self.event_type}:{self.question_content[:100]}".encode()
            ).hexdigest()


class EnhancedEventBuffer:
    """
    增强事件缓冲器。

    特性:
    - 事件先入内存 deque (maxlen=200)
    - 满足以下任一条件时批量刷新:
      1. 缓冲区 ≥ 50 条
      2. 距上次刷新 ≥ 30 秒
      3. 会话结束（外部调用 force_flush）
    - 去重: 5 分钟窗口内 (user_id, event_type, content_hash) 相同则跳过
    - 异步刷新: asyncio.create_task，不阻塞主流程
    """

    def __init__(
        self,
        max_buffer_size: int = 200,
        batch_size: int = 50,
        flush_interval_seconds: int = 30,
        dedup_window_seconds: int = 300,
    ):
        self.max_buffer_size = max_buffer_size
        self.batch_size = batch_size
        self.flush_interval_seconds = flush_interval_seconds
        self.dedup_window_seconds = dedup_window_seconds

        self._buffer: deque[BufferedEvent] = deque(maxlen=max_buffer_size)
        self._last_flush_time = time.time()
        self._seen_hashes: Dict[str, float] = {}
        self._flush_lock = asyncio.Lock()
        self._on_flush_callbacks: List[callable] = []

    async def add(self, event: BufferedEvent) -> bool:
        """添加事件到缓冲区，不阻塞。

        Args:
            event: 缓冲事件

        Returns:
            bool: True 表示已入队
        """
        # 去重检查
        now = time.time()
        if event.content_hash in self._seen_hashes:
            last_seen = self._seen_hashes[event.content_hash]
            if (now - last_seen) < self.dedup_window_seconds:
                logger.debug(f"事件去重: hash={event.content_hash[:8]}")
                return False

        self._seen_hashes[event.content_hash] = now
        self._buffer.append(event)

        # 检查刷新条件
        if len(self._buffer) >= self.batch_size:
            asyncio.create_task(self._flush())
        elif (now - self._last_flush_time) >= self.flush_interval_seconds:
            asyncio.create_task(self._flush())

        # 清理过期的 hash 记录
        self._cleanup_hashes(now)

        return True

    async def force_flush(self) -> int:
        """强制刷新所有缓冲事件（会话结束时调用）。

        Returns:
            int: 实际写入的事件数
        """
        return await self._flush()

    def on_flush(self, callback: callable) -> None:
        """注册刷新回调（用于在批量写入前做分类等预处理）。

        Args:
            callback: async callable，接收 List[BufferedEvent]
        """
        self._on_flush_callbacks.append(callback)

    @property
    def buffer_size(self) -> int:
        return len(self._buffer)

    @property
    def stats(self) -> Dict[str, Any]:
        return {
            "buffer_size": len(self._buffer),
            "last_flush_time": self._last_flush_time,
            "seen_hashes_count": len(self._seen_hashes),
        }

    async def _flush(self) -> int:
        async with self._flush_lock:
            if not self._buffer:
                return 0

            events = list(self._buffer)
            self._buffer.clear()
            self._last_flush_time = time.time()

            logger.info(f"刷新 {len(events)} 条事件到数据库")

            # 执行回调（如事件分类）
            for callback in self._on_flush_callbacks:
                try:
                    await callback(events)
                except Exception as e:
                    logger.warning(f"刷新回调异常: {e}")

            return len(events)

    def _cleanup_hashes(self, now: float):
        expired = [
            h
            for h, t in self._seen_hashes.items()
            if (now - t) >= self.dedup_window_seconds
        ]
        for h in expired:
            del self._seen_hashes[h]
```

### 3.3.3 集成到 `MemoryPersistenceFacade`

在 `agent_core/memory_persistence.py:__init__` 中：

```python
from app.services.event_buffer import EnhancedEventBuffer, BufferedEvent

# __init__ 内追加：
self._event_buffer = EnhancedEventBuffer()

# 新增方法：
async def buffer_event(
    self, user_id: str, event_data: Dict[str, Any]
) -> bool:
    """将事件加入缓冲区（轻量级、不阻塞、不立即写库）。"""
    event = BufferedEvent(
        user_id=user_id,
        event_type=event_data.get("event_type", "unknown"),
        question_content=event_data.get("question_content", ""),
        category=event_data.get("category", ""),
        data=event_data,
    )
    return await self._event_buffer.add(event)

async def flush_buffer(self) -> int:
    """刷新缓冲区（会话结束时调用）。"""
    return await self._event_buffer.force_flush()
```

### 3.3.4 验收

```python
import asyncio
from app.services.event_buffer import EnhancedEventBuffer, BufferedEvent

async def test():
    buf = EnhancedEventBuffer(batch_size=3)

    # 添加 2 条事件 — 不应触发刷新
    assert await buf.add(BufferedEvent("u1", "q", "test 1", "代数", {}))
    assert await buf.add(BufferedEvent("u1", "q", "test 2", "几何", {}))
    assert buf.buffer_size == 2, f"Expected 2, got {buf.buffer_size}"

    # 添加第 3 条 — 应触发批量刷新
    assert await buf.add(BufferedEvent("u1", "q", "test 3", "概率", {}))
    await asyncio.sleep(0.1)
    assert buf.buffer_size == 0, f"Expected 0 after flush, got {buf.buffer_size}"
    print("PASS: 批量刷新触发")

    # 去重测试
    assert await buf.add(BufferedEvent("u1", "q", "test 1", "代数", {}))
    assert await buf.add(BufferedEvent("u1", "q", "test 1", "代数", {}))
    assert buf.buffer_size == 1, "Duplicate should be rejected"
    print("PASS: 去重")

asyncio.run(test())
```

---

## T4a：SkillAggregator（1.5d）

> **注意**：T4a-T4c 与 AI 对话中的 "Skill 机制" 对应。详见第四章。

---

## T4b：MathSkillDAG（1d）

> 详见第四章。

---

## T4c：DifficultyEstimator（2d）

> 详见第四章。

---

## T5：Intent → 持久化策略映射（1d）

### 3.5.1 目标

根据 A-01 意图分类结果，决定事件的持久化策略。

### 3.5.2 修改文件 `agent_core/memory_persistence.py`

在 `MemoryPersistenceFacade` 中新增方法：

```python
    # 静态配置表
    INTENT_PERSISTENCE_MAP = {
        "problem_solving": {
            "persist": True, "ttl_days": 90,
            "record_detail": "full",
        },
        "concept_inquiry": {
            "persist": True, "ttl_days": 90,
            "record_detail": "summary",
        },
        "error_analysis": {
            "persist": True, "ttl_days": 180,
            "record_detail": "full",
        },
        "exam_practice": {
            "persist": True, "ttl_days": 180,
            "record_detail": "aggregated",
        },
        "casual_chat": {
            "persist": False, "ttl_days": 0,
            "record_detail": "none",
        },
    }

    def should_persist(self, intent_type: str) -> bool:
        """判断给定意图是否应持久化。"""
        config = self.INTENT_PERSISTENCE_MAP.get(intent_type, {})
        return config.get("persist", True)

    def get_persistence_config(
        self, intent_type: str
    ) -> Dict[str, Any]:
        """获取持久化配置。"""
        return self.INTENT_PERSISTENCE_MAP.get(
            intent_type,
            {"persist": True, "ttl_days": 90, "record_detail": "full"},
        )
```

### 3.5.3 集成到 `agent.py:_select_strategy`

在 `_select_strategy` 返回前（L952），将 `intent.task_type` 传递给 facade：

```python
# agent.py _select_strategy 调用完成后，调用方处理：
# 在 process() 和 stream() 的 record_event 调用中增加：
persist_config = self._persistence_facade.get_persistence_config(
    intent_type.value
)
if persist_config["persist"]:
    event_data["metadata"]["intent_type"] = intent_type.value
    event_data["metadata"]["ttl_days"] = persist_config["ttl_days"]
```

### 3.5.4 验收

```python
from agent_core.memory_persistence import MemoryPersistenceFacade

facade = MemoryPersistenceFacade()

assert facade.should_persist("problem_solving") == True
assert facade.should_persist("casual_chat") == False
assert facade.get_persistence_config("error_analysis")["ttl_days"] == 180
print("T5 OK")
```

---

## T6：Prometheus 运维指标（1.5d）

### 3.6.1 目标

对记忆持久化全链路增加可观测性。

### 3.6.2 新建文件 `agent_core/metrics.py`

```python
from __future__ import annotations

from prometheus_client import (
    Counter,
    Histogram,
    Gauge,
    CollectorRegistry,
    generate_latest,
)

MEMORY_REGISTRY = CollectorRegistry()

memory_events_total = Counter(
    "memory_events_total",
    "Total memory events written",
    ["event_type", "status"],
    registry=MEMORY_REGISTRY,
)

memory_retrieval_latency = Histogram(
    "memory_retrieval_latency_seconds",
    "Memory retrieval latency",
    ["source"],
    buckets=[0.01, 0.05, 0.1, 0.25, 0.5, 1.0, 2.0],
    registry=MEMORY_REGISTRY,
)

memory_buffer_size = Gauge(
    "memory_buffer_size",
    "Current event buffer size",
    registry=MEMORY_REGISTRY,
)

memory_classification_latency = Histogram(
    "memory_classification_latency_seconds",
    "LLM classification latency",
    buckets=[0.1, 0.25, 0.5, 1.0, 2.0, 5.0],
    registry=MEMORY_REGISTRY,
)

memory_db_write_errors = Counter(
    "memory_db_write_errors_total",
    "Database write error count",
    registry=MEMORY_REGISTRY,
)


def get_metrics_response() -> bytes:
    """返回 Prometheus text 格式的指标"""
    return generate_latest(MEMORY_REGISTRY)
```

### 3.6.3 埋点位置

在 `MemoryPersistenceFacade.record_event` 中：

```python
# 成功时:
memory_events_total.labels(
    event_type=event_data.get("event_type", "unknown"), status="success"
).inc()

# 失败时:
memory_events_total.labels(
    event_type=event_data.get("event_type", "unknown"), status="error"
).inc()
memory_db_write_errors.inc()
```

在 `EnhancedEventBuffer._flush` 中：

```python
memory_buffer_size.set(len(self._buffer))
```

### 3.6.4 暴露 /metrics（可选）

在 `app/main.py` 中新增路由：

```python
from agent_core.metrics import get_metrics_response

@app.get("/metrics")
async def metrics():
    from fastapi.responses import Response
    return Response(
        content=get_metrics_response(),
        media_type="text/plain; charset=utf-8",
    )
```

---

## T7：专项测试（2d）

### 3.7.1 新建文件 `tests/test_memory_persistence.py`

**测试清单（17 个用例）**：

```python
"""
A03 记忆持久化专项测试
运行: pytest tests/test_memory_persistence.py -v
"""

import asyncio
import time
import pytest
from app.services.event_buffer import EnhancedEventBuffer, BufferedEvent
from app.services.memory import (
    LongTermMemory, EventClassification, ShortTermMemory, MemoryItem
)
from agent_core.memory_persistence import MemoryPersistenceFacade, UserProfile


# ========== EventBuffer 测试 (T3) ==========

class TestEventBuffer:
    @pytest.mark.asyncio
    async def test_add_below_threshold(self):
        buf = EnhancedEventBuffer(batch_size=5)
        for i in range(3):
            await buf.add(BufferedEvent("u1", "q", f"test {i}", "代数", {}))
        assert buf.buffer_size == 3

    @pytest.mark.asyncio
    async def test_flush_on_batch_full(self):
        buf = EnhancedEventBuffer(batch_size=2)
        await buf.add(BufferedEvent("u1", "q", "test 1", "代数", {}))
        await buf.add(BufferedEvent("u1", "q", "test 2", "代数", {}))
        await asyncio.sleep(0.2)
        assert buf.buffer_size == 0

    @pytest.mark.asyncio
    async def test_dedup_same_content(self):
        buf = EnhancedEventBuffer(batch_size=10)
        assert await buf.add(BufferedEvent("u1", "q", "same", "代数", {}))
        assert not await buf.add(BufferedEvent("u1", "q", "same", "代数", {}))
        assert buf.buffer_size == 1

    @pytest.mark.asyncio
    async def test_dedup_different_user(self):
        buf = EnhancedEventBuffer(batch_size=10)
        assert await buf.add(BufferedEvent("u1", "q", "same", "代数", {}))
        assert await buf.add(BufferedEvent("u2", "q", "same", "代数", {}))
        assert buf.buffer_size == 2

    @pytest.mark.asyncio
    async def test_force_flush(self):
        buf = EnhancedEventBuffer(batch_size=100)
        for i in range(5):
            await buf.add(BufferedEvent("u1", "q", f"test {i}", "代数", {}))
        assert buf.buffer_size == 5
        count = await buf.force_flush()
        assert count == 5
        assert buf.buffer_size == 0

    @pytest.mark.asyncio
    async def test_buffer_max_size(self):
        buf = EnhancedEventBuffer(max_buffer_size=3, batch_size=100)
        for i in range(5):
            await buf.add(BufferedEvent("u1", "q", f"test {i}", "代数", {}))
        assert buf.buffer_size <= 3


# ========== EventClassification 测试 (T2) ==========

class TestEventClassification:
    def test_fallback_limits(self):
        from app.data.database import get_db_session
        ltm = LongTermMemory(get_db_session)

        result = ltm._classify_fallback({
            "question_content": "求极限 lim(x→0) sinx/x",
            "is_correct": True,
        })
        assert result.category == "极限"

        result = ltm._classify_fallback({
            "question_content": "求积分 ∫ x² dx",
            "is_correct": False,
        })
        assert result.category == "积分"


# ========== Facade 测试 (T1) ==========

class TestMemoryPersistenceFacade:
    @pytest.mark.asyncio
    async def test_profile_type(self):
        facade = MemoryPersistenceFacade()
        profile = await facade.get_profile("test_user")
        assert isinstance(profile, UserProfile)

    @pytest.mark.asyncio
    async def test_profile_dict_has_required_fields(self):
        facade = MemoryPersistenceFacade()
        profile = await facade.get_profile("test_user")
        d = profile.to_dict()
        for field in [
            "total_questions", "correct_rate", "weak_points",
            "strong_points", "skills", "error_patterns",
        ]:
            assert field in d, f"Missing field: {field}"

    @pytest.mark.asyncio
    async def test_compact_json_under_limit(self):
        facade = MemoryPersistenceFacade()
        profile = await facade.get_profile("test_user")
        compact = profile.to_compact_json(max_length=1000)
        assert len(compact) <= 1000

    def test_intent_persistence_map(self):
        facade = MemoryPersistenceFacade()
        assert facade.should_persist("problem_solving") == True
        assert facade.should_persist("casual_chat") == False


# ========== 性能测试 (T7) ==========

class TestPerformance:
    @pytest.mark.asyncio
    async def test_buffer_add_latency(self):
        buf = EnhancedEventBuffer(batch_size=1000)
        latencies = []
        for i in range(100):
            start = time.perf_counter()
            await buf.add(BufferedEvent("u1", "q", f"perf {i}", "代数", {}))
            latencies.append(time.perf_counter() - start)

        avg = sum(latencies) / len(latencies)
        p99 = sorted(latencies)[int(len(latencies) * 0.99)]
        assert avg < 0.01, f"avg latency {avg:.4f}s > 10ms"
        assert p99 < 0.05, f"P99 latency {p99:.4f}s > 50ms"
```

---

## T8：Runbook 运维文档（0.5d）

> 文档框架，AI 开发时根据 T6 的实际指标名称补充具体阈值。

```markdown
# A03 记忆持久化运维手册

## 数据库表
- user_skills: 用户技能熟练度（派生表，可从 learning_records 重建）
- learning_records: 原始学习记录（不可删除）

## 备份策略
- learning_records 每天全量备份
- user_skills 可选择性备份（可从主表重建）

## 关键指标
- memory_events_total{status="error"} > 5/min → 检查数据库连接
- memory_buffer_size > 150 → 强制刷新或扩容
- memory_classification_latency P99 > 2s → 切换关键词降级

## 降级开关
- 环境变量 A03_DISABLE_LLM_CLASSIFY=1 → 关闭 LLM 分类
- 环境变量 A03_DISABLE_BUFFER=1 → 关闭事件缓冲
```

---

# 第四章：Skill 追加机制（T4a / T4b / T4c）

## 4.1 Skill 机制核心概念

```
用户画像 = ErrorPattern（负向）+ Skill（正向）
                                          ↑
                                  本章要实现的内容
```

**Skill** = 用户在某一个具体数学知识点上的能力水平记录。它回答的问题：这个人到底会不会？会到什么程度？

| 概念 | 数据来源 | 存储位置 |
|---|---|---|
| Skill（熟练度） | 聚合 `learning_records` | `user_skills` 表 |
| Error Pattern（错误模式） | 跨知识点关联 | `user_skills` 表 + JSON 字段 |
| Skill DAG（依赖图） | 静态 YAML 配置 | `app/data/math_skill_graph.yml` |

---

## T4a：SkillAggregator（1.5d）

### 4.4.1 目标

从 `learning_records` 表聚合计算每个用户的技能熟练度、错误模式和认知风格。

### 4.4.2 新建文件 `app/services/skill_aggregator.py`

```python
from __future__ import annotations

import json
import logging
from datetime import datetime, timezone, timedelta
from typing import Any, Dict, List, Optional, Tuple

from app.data.database import get_db_session

logger = logging.getLogger(__name__)

# 技能状态阈值
SKILL_THRESHOLDS = {
    "novice": (0.00, 0.30),
    "learning": (0.30, 0.60),
    "proficient": (0.60, 0.85),
    "mastered": (0.85, 1.00),
}


class SkillAggregator:
    """
    从 learning_records 聚合计算用户的技能熟练度。

    核心算法: Elo-style 时间加权评分
    mastery = Σ(time_weight × diff_weight × result) / Σ(time_weight × diff_weight)
    """

    def __init__(self, db_session_factory=None):
        self._session_factory = db_session_factory or get_db_session

    async def get_all_skills(
        self, user_id: str, max_skills: int = 50
    ) -> List[Dict[str, Any]]:
        """获取用户所有技能列表（按熟练度降序）。

        Args:
            user_id: 用户 ID
            max_skills: 最大返回数量

        Returns:
            [{"skill_code":..., "display_name":..., "mastery_level":..., ...}, ...]
        """
        from sqlalchemy import text

        async with self._session_factory() as db:
            # 先查 user_skills 缓存表
            result = await db.execute(
                text(
                    "SELECT skill_code, display_name, category_path, "
                    "mastery_level, status, total_attempts, correct_count, "
                    "recent_streak, best_streak, last_practiced_at, "
                    "first_seen_at, mastered_at, evolution_history "
                    "FROM user_skills WHERE user_id = :uid "
                    "ORDER BY mastery_level DESC LIMIT :limit"
                ),
                {"uid": user_id, "limit": max_skills},
            )
            rows = result.fetchall()

            if rows:
                return [
                    {
                        "skill_code": r[0],
                        "display_name": r[1],
                        "category_path": r[2],
                        "mastery_level": r[3],
                        "status": r[4],
                        "total_attempts": r[5],
                        "correct_count": r[6],
                        "recent_streak": r[7],
                        "best_streak": r[8],
                        "last_practiced": (
                            r[9].isoformat() if r[9] else None
                        ),
                        "first_seen": r[10].isoformat() if r[10] else None,
                        "mastered_at": r[11].isoformat() if r[11] else None,
                        "evolution_history": (
                            json.loads(r[12]) if r[12] else []
                        ),
                    }
                    for r in rows
                ]

            # 缓存未命中 → 实时计算
            return await self._compute_skills(user_id, max_skills)

    async def recalculate_skills(
        self, user_id: str, skill_codes: Optional[List[str]] = None
    ) -> int:
        """重新计算指定 skill 的熟练度并更新 user_skills 表。

        Args:
            user_id: 用户 ID
            skill_codes: 要重算的技能列表，None 则重算全部

        Returns:
            int: 更新的 skill 数量
        """
        skills = await self._compute_skills(user_id)

        filtered = skills
        if skill_codes:
            filtered = [s for s in skills if s["skill_code"] in skill_codes]

        from sqlalchemy import text
        async with self._session_factory() as db:
            for skill in filtered:
                await db.execute(
                    text(
                        "INSERT INTO user_skills (user_id, skill_code, "
                        "display_name, category_path, mastery_level, status, "
                        "total_attempts, correct_count, recent_streak, "
                        "best_streak, first_seen_at, last_practiced_at, "
                        "mastered_at, evolution_history, updated_at) "
                        "VALUES (:uid, :sc, :dn, :cp, :ml, :st, :ta, :cc, "
                        ":rs, :bs, :fs, :lp, :ma, :eh, NOW()) "
                        "ON CONFLICT (user_id, skill_code) DO UPDATE SET "
                        "mastery_level = EXCLUDED.mastery_level, "
                        "status = EXCLUDED.status, "
                        "total_attempts = EXCLUDED.total_attempts, "
                        "correct_count = EXCLUDED.correct_count, "
                        "recent_streak = EXCLUDED.recent_streak, "
                        "best_streak = EXCLUDED.best_streak, "
                        "last_practiced_at = EXCLUDED.last_practiced_at, "
                        "evolution_history = EXCLUDED.evolution_history, "
                        "updated_at = NOW()"
                    ),
                    {
                        "uid": user_id,
                        "sc": skill["skill_code"],
                        "dn": skill["display_name"],
                        "cp": skill.get("category_path", ""),
                        "ml": skill["mastery_level"],
                        "st": skill["status"],
                        "ta": skill["total_attempts"],
                        "cc": skill["correct_count"],
                        "rs": skill["recent_streak"],
                        "bs": skill["best_streak"],
                        "fs": skill.get("first_seen"),
                        "lp": skill.get("last_practiced"),
                        "ma": skill.get("mastered_at"),
                        "eh": json.dumps(
                            skill.get("evolution_history", []),
                            ensure_ascii=False,
                        ),
                    },
                )
            await db.commit()
        return len(filtered)

    async def get_error_patterns(
        self, user_id: str
    ) -> List[Dict[str, Any]]:
        """获取跨技能的错误模式。

        Returns:
            [{"pattern": "符号遗漏", "frequency": 0.73,
              "affected_skills": ["trig_sum", "trig_double"]}, ...]
        """
        from sqlalchemy import text

        async with self._session_factory() as db:
            result = await db.execute(
                text(
                    "SELECT error_reason, COUNT(*) as freq, "
                    "array_agg(DISTINCT category) as affected "
                    "FROM learning_records "
                    "WHERE user_id = :uid AND is_correct = FALSE "
                    "AND error_reason IS NOT NULL AND error_reason != '' "
                    "GROUP BY error_reason "
                    "HAVING COUNT(DISTINCT category) >= 2 "
                    "ORDER BY freq DESC "
                    "LIMIT 10"
                ),
                {"uid": user_id},
            )
            rows = result.fetchall()

            total_errors = sum(r[1] for r in rows) or 1
            return [
                {
                    "pattern": r[0],
                    "frequency": round(r[1] / total_errors, 2),
                    "affected_skills": list(r[2]) if r[2] else [],
                }
                for r in rows
            ]

    async def get_cognitive_style(
        self, user_id: str
    ) -> Dict[str, Any]:
        """推导用户认知风格（初步：基于时间消耗模式）。

        Returns:
            {"style": "visual", "details": {...}} 或空 dict（数据不足）
        """
        from sqlalchemy import text

        async with self._session_factory() as db:
            result = await db.execute(
                text(
                    "SELECT AVG(time_spent), COUNT(*) "
                    "FROM learning_records "
                    "WHERE user_id = :uid AND time_spent IS NOT NULL"
                ),
                {"uid": user_id},
            )
            row = result.fetchone()
            avg_time, count = row[0], row[1]

            if not count or count < 10:
                return {}

            return {
                "avg_time_per_question": round(avg_time or 0, 1),
                "total_analyzed": count,
                "style_hint": (
                    "快速型"
                    if avg_time and avg_time < 60
                    else "深思型"
                    if avg_time and avg_time > 180
                    else "标准型"
                ),
            }

    async def _compute_skills(
        self, user_id: str, limit: int = 50
    ) -> List[Dict[str, Any]]:
        """核心算法：从 learning_records 全量计算所有技能的熟练度。"""
        from sqlalchemy import text

        async with self._session_factory() as db:
            result = await db.execute(
                text(
                    "SELECT DISTINCT sub_categories, category "
                    "FROM learning_records "
                    "WHERE user_id = :uid AND sub_categories IS NOT NULL "
                    "AND sub_categories != ''"
                ),
                {"uid": user_id},
            )
            skill_rows = result.fetchall()

            skills = []
            now = datetime.now(timezone.utc)

            for row in skill_rows:
                sub_cat = row[0]
                category = row[1]
                skill_code = self._derive_skill_code(category, sub_cat)

                # 获取该 skill 的所有记录
                records_result = await db.execute(
                    text(
                        "SELECT is_correct, difficulty, time_spent, "
                        "created_at "
                        "FROM learning_records "
                        "WHERE user_id = :uid "
                        "AND (sub_categories = :sc OR category = :cat) "
                        "ORDER BY created_at DESC"
                    ),
                    {"uid": user_id, "sc": sub_cat, "cat": category},
                )
                records = records_result.fetchall()

                if not records:
                    continue

                mastery, status, streak, best_streak, history = (
                    self._calculate_mastery(records, now)
                )

                types = [r[0] for r in records]
                skills.append(
                    {
                        "skill_code": skill_code,
                        "display_name": sub_cat,
                        "category_path": f"{category} > {sub_cat}",
                        "mastery_level": round(mastery, 3),
                        "status": status,
                        "total_attempts": len(records),
                        "correct_count": sum(1 for t in types if t),
                        "recent_streak": streak,
                        "best_streak": best_streak,
                        "first_seen": records[-1][3].isoformat()
                        if records[-1][3]
                        else None,
                        "last_practiced": records[0][3].isoformat()
                        if records[0][3]
                        else None,
                        "mastered_at": (
                            history[-1]["date"]
                            if history and status == "mastered"
                            else None
                        ),
                        "evolution_history": history,
                    }
                )

            skills.sort(key=lambda s: s["mastery_level"], reverse=True)
            return skills[:limit]

    def _calculate_mastery(
        self,
        records: List[Tuple],
        now: datetime,
    ) -> Tuple[float, str, int, int, List[Dict]]:
        """Elo-style 时间加权评分算法。"""
        weighted_score = 0.0
        total_weight = 0.0
        streak = 0
        best_streak = 0
        current_streak = 0
        history_points = []

        for i, (is_correct, difficulty, spent, created) in enumerate(
            reversed(records)
        ):
            days_ago = (
                now - (created or now.replace(tzinfo=None)).replace(
                    tzinfo=timezone.utc
                )
            ).days

            time_weight = 0.5 ** (days_ago / 14)
            diff_weight = 0.5 + ((difficulty or 3) / 10)
            result_score = 1.0 if is_correct else 0.0

            combined = time_weight * diff_weight * result_score
            weighted_score += combined
            total_weight += time_weight * diff_weight

            if is_correct:
                current_streak += 1
                best_streak = max(best_streak, current_streak)
            else:
                current_streak = 0

            if (i + 1) % 5 == 0 or i == len(records) - 1:
                mastery = (
                    weighted_score / total_weight
                    if total_weight > 0
                    else 0.0
                )
                history_points.append(
                    {
                        "date": created.isoformat() if created else "",
                        "mastery": round(mastery, 3),
                        "attempts": i + 1,
                    }
                )

        mastery = (
            weighted_score / total_weight if total_weight > 0 else 0.0
        )
        status = self._determine_status(mastery)
        return mastery, status, current_streak, best_streak, history_points

    @staticmethod
    def _determine_status(mastery: float) -> str:
        for status, (lo, hi) in SKILL_THRESHOLDS.items():
            if lo <= mastery < hi:
                return status
        return "mastered"

    @staticmethod
    def _derive_skill_code(category: str, sub_category: str) -> str:
        import re
        raw = f"{category}_{sub_category}".lower()
        return re.sub(r"[^a-z0-9_]", "_", raw)[:50]
```

### 4.4.3 集成到 `MemoryPersistenceFacade`

```python
# agent_core/memory_persistence.py L60-L64 __init__ 修改为：

def __init__(self, db_session_factory=None):
    self._session_factory = db_session_factory or get_db_session
    self._long_term = LongTermMemory(self._session_factory)
    from app.services.skill_aggregator import SkillAggregator
    self._skill_aggregator = SkillAggregator(self._session_factory)
    from app.services.event_buffer import EnhancedEventBuffer
    self._event_buffer = EnhancedEventBuffer()
    logger.info("MemoryPersistenceFacade 初始化完成（含 SkillAggregator + EventBuffer）")
```

---

## T4b：MathSkillDAG（1d）

### 4.4.4 新建文件 `app/services/math_skill_dag.py`

```python
"""数学技能依赖图（静态 DAG，基于 math_skill_graph.yml）"""

import os
import yaml


class MathSkillDAG:
    def __init__(self, yaml_path: str = None):
        if yaml_path is None:
            yaml_path = os.path.join(
                os.path.dirname(__file__),
                "..", "data", "math_skill_graph.yml",
            )
        with open(yaml_path, "r", encoding="utf-8") as f:
            self._graph = yaml.safe_load(f)

    def get_skill(self, code: str) -> dict:
        return self._graph.get(code, {})

    def get_prerequisites(self, code: str) -> list:
        return self._graph.get(code, {}).get("prerequisites", [])

    def get_unlocks(self, code: str) -> list:
        """获取某技能解锁的下一级技能"""
        unlocks = []
        for key, node in self._graph.items():
            if code in node.get("prerequisites", []):
                unlocks.append(key)
        return unlocks

    def can_learn(self, code: str, mastered_skills: set) -> bool:
        """判断用户是否满足某项技能的所有先修条件"""
        prereqs = self.get_prerequisites(code)
        return all(p in mastered_skills for p in prereqs) if prereqs else True

    def get_next_unlockable(
        self, mastered_skills: set, learning_skills: set
    ) -> list:
        """获取用户当前可以解锁但尚未开始学习的技能"""
        unlockable = []
        for code, node in self._graph.items():
            if code in mastered_skills or code in learning_skills:
                continue
            prereqs = node.get("prerequisites", [])
            if all(p in mastered_skills for p in prereqs):
                unlockable.append({"code": code, "node": node})
        return unlockable

    def get_all_skill_codes(self) -> list:
        return list(self._graph.keys())
```

### 4.4.5 集成到 `SkillAggregator`

```python
# app/services/skill_aggregator.py __init__ 中注入：
from app.services.math_skill_dag import MathSkillDAG

class SkillAggregator:
    def __init__(self, db_session_factory=None):
        # ... existing ...
        self._skill_dag = MathSkillDAG()

    async def get_next_unlockable(self, user_id: str) -> List[Dict]:
        """获取用户可解锁的下一级技能"""
        skills = await self.get_all_skills(user_id)
        mastered_ids = {
            s["skill_code"] for s in skills if s["status"] == "mastered"
        }
        learning_ids = {
            s["skill_code"] for s in skills if s["status"] != "mastered"
        }
        return self._skill_dag.get_next_unlockable(
            mastered_ids, learning_ids
        )

    async def get_prerequisite_check(
        self, user_id: str, skill_code: str
    ) -> Dict[str, Any]:
        """检查用户是否满足某技能的先修条件"""
        skills = await self.get_all_skills(user_id)
        mastered_ids = {
            s["skill_code"] for s in skills if s["status"] == "mastered"
        }
        can = self._skill_dag.can_learn(skill_code, mastered_ids)
        missing = [
            p
            for p in self._skill_dag.get_prerequisites(skill_code)
            if p not in mastered_ids
        ]
        return {"can_learn": can, "missing_prerequisites": missing}
```

---

## T4c：DifficultyEstimator（2d）

### 4.4.6 新建文件 `app/services/difficulty_estimator.py`

```python
"""动态难度估算器 — 替代 LongTermMemory.get_user_profile() 中的硬编码映射"""

import logging
from typing import Any, Dict, List, Optional

logger = logging.getLogger(__name__)


class DifficultyEstimator:
    """
    基于用户画像 + Skill 熟练度的动态难度估算。

    算法: 加权评分 → 映射到 1-5 分制

    score = 0.4 × category_mastery(category)
          + 0.3 × overall_correct_rate
          + 0.2 × recent_trend(category, window=10)
          + 0.1 × time_since_last_practice(category)
    """

    def __init__(self, skill_aggregator=None):
        self._skill_aggregator = skill_aggregator

    async def estimate(
        self,
        user_id: str,
        category: str,
        sub_category: str = "",
        profile: Optional[Dict[str, Any]] = None,
    ) -> int:
        """
        估算用户的推荐难度。

        Args:
            user_id: 用户 ID
            category: 目标知识点分类
            sub_category: 子分类（用于精确匹配 skill）
            profile: 可选的用户画像（避免重复查询）

        Returns:
            int: 推荐难度 1-5
        """
        skill_code = (
            f"{category}_{sub_category}".lower().replace(" ", "_")
            if sub_category
            else ""
        )

        # 因子 1: 该知识点的掌握度 (0-1)
        category_mastery = 0.0
        if self._skill_aggregator and skill_code:
            skills = await self._skill_aggregator.get_all_skills(user_id)
            for s in skills:
                if s["skill_code"] == skill_code:
                    category_mastery = s["mastery_level"]
                    break

        # 因子 2: 整体正确率 (0-1)
        overall_rate = (
            profile.get("correct_rate", 0.5) if profile else 0.5
        )

        # 因子 3: 近期趋势
        recent_trend = 0.5  # 默认中性（T7 后可通过 evolution_history 计算）

        # 因子 4: 遗忘因子
        time_factor = 0.5  # 默认中性（T7 后可基于 last_practiced 计算）

        # 加权评分: 越低 → 越推荐低难度
        score = (
            0.4 * category_mastery
            + 0.3 * overall_rate
            + 0.2 * recent_trend
            + 0.1 * time_factor
        )

        difficulty = self._score_to_difficulty(score)
        logger.info(
            f"难度估算: mastery={category_mastery:.2f} overall={overall_rate:.2f} "
            f"trend={recent_trend:.2f} time_f={time_factor:.2f} → score={score:.2f} → d={difficulty}"
        )
        return difficulty

    @staticmethod
    def _score_to_difficulty(score: float) -> int:
        """score 0-1 → 难度 1-5"""
        if score > 0.85:
            return 5
        elif score > 0.65:
            return 4
        elif score > 0.45:
            return 3
        elif score > 0.25:
            return 2
        else:
            return 1
```

### 4.4.7 集成到 `get_user_profile()`

修改 `app/services/memory.py:L212-L219`（硬编码的 recommended_difficulty）：

```python
# 替换前 (L212-L219):
# if correct_rate > 0.8: recommended_difficulty = 5
# elif correct_rate > 0.6: recommended_difficulty = 4
# ...

# 替换后:
# recommended_difficulty 保持基础值（作为后端兼容）
# 真正的动态难度由 agent.py 通过 Facade 调用 DifficultyEstimator 获取
```

### 4.4.8 验收

```python
import asyncio
from app.services.difficulty_estimator import DifficultyEstimator

async def test():
    est = DifficultyEstimator()
    d = await est.estimate("test_user", "三角函数", "和角公式")
    assert 1 <= d <= 5, f"Expected 1-5, got {d}"
    print(f"估计难度: {d}")

    d2 = await est.estimate("test_user", "极限", "",
                             profile={"correct_rate": 0.95})
    assert d2 >= d, "High correct rate should suggest higher difficulty"
    print(f"高正确率 → 难度: {d2}")
    print("T4c OK")

asyncio.run(test())
```

---

# 第五章：附录

## 5.1 验收检查 Python 脚本

```bash
# 运行完整的 T0-T4c 验收检查
python tests/test_memory_persistence.py -v

# 单独验证 Facade
python -c "
import asyncio
from agent_core.memory_persistence import MemoryPersistenceFacade

async def main():
    f = MemoryPersistenceFacade()
    p = await f.get_profile('test_user')
    d = p.to_dict()
    required = ['total_questions','correct_rate','weak_points','strong_points','skills','error_patterns']
    for r in required:
        assert r in d, f'Missing: {r}'
    assert len(p.to_compact_json(1000)) <= 1000
    print('FACADE OK')
asyncio.run(main())
"
```

## 5.2 Git 提交策略（含验证门禁）

### ❌ 禁止的做法：每个 Task 都 commit

```
# 不要这样做！
git add . && git commit -m "[A03-T0] feat: ..."   ← T0 写完就提交
git add . && git commit -m "[A03-T1] feat: ..."   ← T1 写完又提交
# 如果 T1 有 bug，系统已经跑不起来了，只能回滚
```

### ✅ 推荐做法：分批提交 + 提交前必须通过验证门禁

```
┌─────────────────────────────────────────────────────┐
│                  A03 开发提交流程                    │
│                                                     │
│  批次 1 (基础设施):                                  │
│    完成 T0 → 运行验证门禁 V0 → 通过 → commit        │
│                                                     │
│  批次 2 (核心功能):                                  │
│    完成 T1+T2 → 运行验证门禁 V1 → 通过 → commit      │
│              ↓ 不通过 → 修复后重新跑 V1             │
│                                                     │
│  批次 3 (增强层):                                    │
│    完成 T3+T4a+T4b+T4c → 运行验证门禁 V2 → 通过     │
│                         → commit                   │
│                                                     │
│  批次 4 (集成与运维):                                │
│    完成 T5+T6+T7+T8 → 运行验证门禁 V3(全量)         │
│                       → 通过 → final commit         │
└─────────────────────────────────────────────────────┘
```

### 验证门禁脚本

**文件**：`scripts/a03_verify.py`（新建，放在项目根目录 `scripts/` 下）

```python
"""
A03 开发验证门禁 — 每次提交前必须运行并通过
用法: python scripts/a03_verify.py --level V0|V1|V2|V3

V0 = 基础设施 (T0 完成后)
V1 = 核心功能 (T1+T2 完成后)
V2 = 增强层 (T3+T4 完成后)
V3 = 全量 (所有 Task 完成后)
"""

import sys
import asyncio
import traceback


def check_import(module_path: str, name: str) -> bool:
    """检查模块能否正常导入"""
    try:
        __import__(module_path)
        print(f"  ✅ {name}")
        return True
    except Exception as e:
        print(f"  ❌ {name}: {e}")
        return False


async def check_async_init(name: str, init_func) -> bool:
    """检查异步初始化是否成功"""
    try:
        await init_func()
        print(f"  ✅ {name} (async)")
        return True
    except Exception as e:
        print(f"  ❌ {name}: {e}")
        return False


def check_db_table(table_name: str) -> bool:
    """检查数据库表是否存在"""
    try:
        from app.data.database import get_db_session
        import asyncio as aio

        async def _check():
            async with get_db_session() as db:
                result = await db.execute(
                    __import__("sqlalchemy").text(
                        f"SELECT COUNT(*) FROM {table_name}"
                    )
                )
                count = result.scalar()
                return True
        return aio.run(_check())
    except Exception as e:
        if "does not exist" in str(e).lower() or "no such table" in str(e).lower():
            print(f"  ⚠️  表 {table_name} 不存在（可能尚未执行迁移）")
            return False
        raise


async def run_v0():
    """V0: T0 完成后 — 数据库 + ORM"""
    print("\n🔍 V0 验证: 基础设施")
    results = []
    results.append(check_import("app.data.models", "UserSkill ORM"))
    results.append(check_db_table("user_skills"))
    return all(results)


async def run_v1():
    """V1: T1+T2 完成后 — Facade + 分类"""
    print("\n🔍 V1 验证: 核心功能")
    results = []
    results.append(await run_v0())
    results.append(check_import("agent_core.memory_persistence", "MemoryPersistenceFacade"))
    results.append(check_import("agent_core.memory_persistence", "UserProfile"))

    # 测试 Facade 初始化和基本操作
    try:
        from agent_core.memory_persistence import MemoryPersistenceFacade
        facade = MemoryPersistenceFacade()
        profile = await facade.get_profile("__verify_test__")
        assert hasattr(profile, 'to_dict')
        assert 'total_questions' in profile.to_dict()
        assert len(profile.to_compact_json(1000)) <= 1000
        print("  ✅ Facade.get_profile() 正常")
        results.append(True)

        ok = await facade.record_event('__verify_test__', {
            'event_type': 'answer_correct',
            'question_content': '测试题目',
            'category': '代数',
            'is_correct': True,
        })
        print(f"  ✅ Facade.record_event() → {ok}")
        results.append(True)
    except Exception as e:
        print(f"  ❌ Facade 操作异常: {e}")
        results.append(False)
        results.append(False)

    # 测试关键词分类降级路径
    try:
        from app.services.memory import LongTermMemory
        from app.data.database import get_db_session
        ltm = LongTermMemory(get_db_session)
        r = ltm._classify_fallback({
            'question_content': '求极限 lim(x→0) sinx/x',
            'is_correct': True,
        })
        assert r.category == "极限"
        print("  ✅ _classify_fallback 关键词降级")
        results.append(True)
    except Exception as e:
        print(f"  ❌ 分类降级: {e}")
        results.append(False)

    return all(results)


async def run_v2():
    """V2: T3+T4 完成后 — 缓冲 + Skill"""
    print("\n🔍 V2 验证: 增强层")
    results = []
    results.append(await run_v1())

    # EventBuffer
    results.append(check_import("app.services.event_buffer", "EnhancedEventBuffer"))
    try:
        import asyncio as aio
        from app.services.event_buffer import EnhancedEventBuffer, BufferedEvent
        buf = EnhancedEventBuffer(batch_size=3)
        for i in range(3):
            await buf.add(BufferedEvent("v", "q", f"t{i}", "x", {}))
        await aio.sleep(0.15)
        assert buf.buffer_size == 0
        print("  ✅ EventBuffer 批量刷新")
        results.append(True)
    except Exception as e:
        print(f"  ❌ EventBuffer: {e}")
        results.append(False)

    # SkillAggregator
    results.append(check_import("app.services.skill_aggregator", "SkillAggregator"))
    results.append(check_import("app.services.math_skill_dag", "MathSkillDAG"))
    results.append(check_import("app.services.difficulty_estimator", "DifficultyEstimator"))

    # DAG 加载
    try:
        from app.services.math_skill_dag import MathSkillDAG
        dag = MathSkillDAG()
        codes = dag.get_all_skill_codes()
        assert len(codes) >= 20
        assert dag.can_learn("basic_operations", set())
        assert not dag.can_learn("trig_sum_formula", set())
        print(f"  ✅ MathSkillDAG ({len(codes)} 个节点)")
        results.append(True)
    except Exception as e:
        print(f"  ❌ MathSkillDAG: {e}")
        results.append(False)

    # DifficultyEstimator
    try:
        from app.services.difficulty_estimator import DifficultyEstimator
        est = DifficultyEstimator()
        d = await est.estimate("__verify__", "三角函数", "和角公式")
        assert 1 <= d <= 5
        print(f"  ✅ DifficultyEstimator → d={d}")
        results.append(True)
    except Exception as e:
        print(f"  ❌ DifficultyEstimator: {e}")
        results.append(False)

    return all(results)


async def run_v3():
    """V3: 全量 — 含系统启动 + pytest"""
    print("\n🔍 V3 验证: 全量")
    results = []
    results.append(await run_v2())

    # Prometheus metrics
    results.append(check_import("agent_core.metrics", "get_metrics_response"))

    # Intent 映射
    try:
        from agent_core.memory_persistence import MemoryPersistenceFacade
        facade = MemoryPersistenceFacade()
        assert facade.should_persist("problem_solving") == True
        assert facade.should_persist("casual_chat") == False
        assert facade.get_persistence_config("error_analysis")["ttl_days"] == 180
        print("  ✅ Intent→Persistence 映射")
        results.append(True)
    except Exception as e:
        print(f"  ❌ Intent 映射: {e}")
        results.append(False)

    # 系统启动检查（导入 main 不触发启动，只验证无循环依赖）
    results.append(check_import("main", "FastAPI app"))

    print("\n📋 V3 建议: 运行 pytest tests/test_memory_persistence.py -v")
    print("   （pytest 需要单独运行，此处仅做模块级检查）")

    return all(results)


LEVELS = {
    "V0": ("T0 完成后", run_v0),
    "V1": ("T1+T2 完成后", run_v1),
    "V2": ("T3+T4 完成后", run_v2),
    "V3": ("全部完成后", run_v3),
}


async def main():
    level = sys.argv[1] if len(sys.argv) > 1 else "V3"

    if level not in LEVELS:
        print(f"❌ 无效级别: {level}, 可选: {list(LEVELS.keys())}")
        sys.exit(1)

    desc, fn = LEVELS[level]
    print(f"=" * 50)
    print(f"A03 验证门禁 — Level {level} ({desc})")
    print(f"=" * 50)

    try:
        passed = await fn()
        print(f"\n{'=' * 50}")
        if passed:
            print(f"✅ Level {level} 全部通过 — 可以安全提交")
            print(f"=" * 50)
            sys.exit(0)
        else:
            print(f"❌ Level {level} 未通过 — 请修复后再提交")
            print(f"=" * 50)
            sys.exit(1)
    except Exception as e:
        print(f"\n💥 验证脚本自身异常: {e}")
        traceback.print_exc()
        sys.exit(2)


if __name__ == "__main__":
    asyncio.run(main())
```

### 使用方式

```bash
# ====== 开发流程示例 ======

# Step 1: 完成 T0（DDL + ORM）
# 编辑完代码后：
python scripts/a03_verify.py --level V0
# 输出: ✅ Level V0 全部通过 → 可以安全提交
git add .
git commit -m "[A03-batch1] T0: user_skills DDL + ORM + math_skill_graph.yml"

# Step 2: 完成 T1 + T2（Facade + LLM 分类）
python scripts/a03_verify.py --level V1
# 输出: ✅ Level V1 全部通过 → 可以安全提交
git add .
git commit -m "[A03-batch2] T1+T2: MemoryPersistenceFacade + LLM event classification"

# Step 3: 完成 T3 + T4a + T4b + T4c（缓冲 + Skill）
python scripts/a03_verify.py --level V2
# 输出: ✅ Level V2 全部通过 → 可以安全提交
git add .
git commit -m "[A03-batch3] T3+T4: EventBuffer + SkillAggregator + MathSkillDAG + DifficultyEstimator"

# Step 4: 完成 T5-T8（映射 + 指标 + 测试 + 文档）
python scripts/a03_verify.py --level V3
# 输出: ✅ Level V3 全部通过 → 可以安全提交
git add .
git commit -m "[A03-batch4] T5-T8: Intent mapping + Prometheus + tests + runbook"

# ====== 如果某步失败 ======
# python scripts/a03_verify.py --level V1
# 输出: ❌ Level V1 未通过
#   ✅ UserSkill ORM
#   ✅ MemoryPersistenceFacade
#   ❌ Facade.record_event(): DatabaseError: relation "learning_records" does not exist
#
# → 修复 bug → 重新运行 → 通过后才 commit
```

### Git 分支策略

```bash
# 从主分支创建 A03 功能分支
git checkout -b feature/A03-memory-persistence

# 所有批次提交都在此分支上
# [batch1] ... [batch2] ... [batch3] ... [batch4]

# 全部完成且 V3 通过后：
git push origin feature/A03-memory-persistence
# 创建 PR 合入主分支
```

---

## 5.3 快速启动检查清单

开发完成后，按以下顺序验证：

- [ ] `python -c "from app.data.models import UserSkill"` — ORM 加载成功
- [ ] `python -c "from agent_core.memory_persistence import MemoryPersistenceFacade"` — Facade 可导入
- [ ] `python -c "from app.services.event_buffer import EnhancedEventBuffer"` — EventBuffer 可导入
- [ ] `python -c "from app.services.skill_aggregator import SkillAggregator"` — SkillAggregator 可导入
- [ ] `python -c "from app.services.difficulty_estimator import DifficultyEstimator"` — Estimator 可导入
- [ ] `python -c "from app.services.math_skill_dag import MathSkillDAG"` — DAG 可导入
- [ ] `python -c "from agent_core.metrics import get_metrics_response"` — Metrics 可导入
- [ ] `pytest tests/test_memory_persistence.py -v` — 17/17 测试通过

---

> **开发完毕。本规格书已包含所有 AI 开发所需的精确信息。按 T0→T1→...→T8 顺序执行即可。**